# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

SummaRace is an **offline Android reading game** (Unity 6 / URP 17.4.0, portrait, new Input System) teaching Grade-4 learners to summarize stories via the **SWBST framework** (Somebody·Wanted·But·So·Then). It is a **college thesis instrument** — 40 learners will use it across 10 sessions and be compared to a control group.

**Read the docs in `Documentation/` before making design decisions:**
- `SummaRace_Project_Brief.md` — orientation (read first)
- `SummaRace_Final_GDD.docx` — design bible; all decisions D1–D18 are LOCKED
- `SummaRace_Technical_Design_Document.md` — implementation spec (scenes, scripts, events, race system)
- `SummaRace_Build_Guide.md` — build order (phases A–J)
- `SummaRace_Asset_Requirements_List.md` — asset bill of materials
- `Documentation/Mockups/1..27.png` — validated prototype screens (visual target). Live demo: https://tdrx44.csb.app/

**Non-negotiables:** 100% offline (no networking/ads/analytics), never punish the learner (wrong answers never block; "caught" is friendly, never game-over), content is JSON data never code, all 30 stories flow through one reusable engine.

## Build state (updated 2026-07-16)

| Phase | Status |
|---|---|
| B skeleton, C core systems, D content pipeline | ✅ done |
| **E vertical slice (M1)** — full loop Boot→MainMenu→StorySelect→Reader→Race→Arrange→Summary→Results, grey-box, sounds wired | ✅ **code-complete**; full-loop playtest by owner pending |
| **F1 typography** — all UI on TextMeshPro; TMP assets in `Art/Fonts/TMP/` (Fredoka-SemiBold = headings/buttons, Nunito-Regular = body + TMP default, Nunito-Bold = feedback); Summary uses TMP_InputField; Results stars are Hyper_Casual_UI sprite Images | ✅ done (verify in playtest) |
| **F2 UI skin** — Hyper_Casual_UI kit applied: 9-sliced pill buttons + Rectangle 356 panels everywhere, sky/curved-hill backdrops (generated `_Game/Art/UI/hill_arc.png`), race banner on toggled BannerBG pill | ✅ done (verify in playtest) |
| **F3 juice + bordered panels** — major cards on the kit's bordered popup sprites (gold `Daily Reward pannel`, teal `Frame 1583`); `SummaRace.UI.ButtonSquash` on every button, `PanelIntro` pop-in on cards/popups; PrimeTween PunchScale on stars + race feedback | ✅ done (verify in playtest) |
| **F4 race reskin** — `_Game/Prefabs/PlayerCharacter` (Ch46 kid) + `PatrolCharacter` (Ty, ZombieRun) spawn via `RaceController.playerModelPrefab/patrolModelPrefab` (grey-box fallback if unwired); AnimatorControllers in `_Game/Animation/` (Running bool, Stumble/Dance triggers); grass track w/ blue-yellow-green lanes | ✅ done (verify in playtest) |
| **F4b world dressing** — Supercyan forest props (URP-converted) line the race track via `RaceController.sceneryPrefabs`/`BuildScenery`, deterministic scatter, 2x scale | ✅ done (verify in playtest) |
| **F5 hero + world labels** — StorySelect easy card shows hero image + title from story JSON (missing art → title-only fallback); race world text is 3D TMP (Fredoka) on dark backing quads | ✅ done (verify in playtest) |
| **F6 eye-candy** — gradient backgrounds all UI scenes (`v_gradient(_soft).png`), gold-gradient floating title w/ `Fredoka…Display.mat` underlay shadow, `RaceChaseCamera` (lane follow + boost FOV kick via `RaceController.SpeedMultiplier`) | ✅ done (verify in playtest) |
| **F7 playtest fixes** — KI natural run + animator-speed sync; loading overlay = gradient + gold tip card; CFXR FX in race (collect/wrong/caught POW/finish fireworks); dark border rings on CTA buttons; teacher avatar + speech bubble on Arrange/Summary (TEMP crop from mockup 21); pickups bob | ✅ done (verify in playtest) |
| **F8 race environment** — Supercyan skybox (shader restored — do NOT batch-convert `Skybox/*` mats) + linear fog; SimplePoly fence lines both sides; unlit puffy sphere clouds; 14 scenery slots incl. benches/lights/flowers/ithappy animals; EnergyShell glow on correct pickup; 72 SimplePoly+NaturePack mats URP-converted | ✅ done (verify in playtest) |
| **F9 runner feel** — subway-runner camera (0,3.3,-5.4 @13°, FOV 64/74); SmoothCameraShaker RockImpact on caught; KI LeanLeft/Right on lane switch (PlayerRunner→`RaceController.OnLaneSwitched`); +6 Kenney sounds (press tap, panel pop, transition chime, 3 speed-synced grass footsteps) | ✅ done (verify in playtest) |
| **F11 race readability** — world text = 9-sliced kit-sprite cards (`RaceController.BuildWorldCard`, SpriteRenderer): white answer cards ARE the pickups (invisible trigger cubes), yellow SWBST pill + FINISH card; gold-tint highlight; collect = fly-up pop; recycle clamped (no gate stacking); Boot splash styled; hills on 4 scenes | ✅ done |
| **F12 patrol/city/icons** — patrol range fixed 1.8–12.8m so it *looms into camera view* at high danger (was always behind the camera); sun aimed down-road; lock icons (`Hyper_Casual_UI/Sprites/Icons/lock.png`) on locked cards; Layer Lab trophy on Results; **Cartoon City FREE** in `Plugins/ithappy/` → `BuildSkyline()` buildings + palms/traffic lights/cars in scenery; 79 model importers fixed | ✅ done |
| **F13 game backdrop** — `_Game/Art/UI/bg_playground.png` = the 3D world rendered + soft-blurred, used as Reader/Arrange/Summary background (mockup-4 style, per-scene tints; Hill disabled there) | ✅ done |
| **F14 boot splash v2 + race pacing** — Boot rebuilt: `bg_playground` backdrop, 150pt gold title + `GameText.BootTagline` + treasure decor (Gems and gold pack chest/coins/gems, UIFloat bob + PanelIntro pop), `GameRules.SplashSeconds` (2s); `Bootstrapper` sets tagline/loading text from GameText; s01 `checkpointSpacing` 30→45 (~47s run, ~8s reading per gate). New packs moved to `Plugins/`: `20 Logos PSD` (title PNGs have baked names — PSD edit needed for a SummaRace logo; fonts + sunburst bg usable), `Game GUI Vol1` (4 icon-button themes; Playful fits — candidates: VOICE ON/OFF Sound icons, Pause in race), `Gems and gold` (coins/diamonds/chests; 512 PNGs; importers default to Texture — set Sprite before use) | ✅ done (verify in playtest) |
| **F15 adventure theme pass** — race lanes → natural dirt trail (matte tan bed + packed-earth edges + soft cream dashed guides, shared mats), `RaceController.tracksideObstaclePrefabs` (5 SimpleNaturePack rocks/bush/mushroom/stump @2.2x on trail shoulder, colliders stripped, skips checkpoint zones); Boot: kit crown icon over title (royal-logo look) + splash progress bar (`Bootstrapper.splashFill`); SceneLoader overlay got real async progress bar (generated `Resources/UI/bar_bg/bar_fill` pill sprites, capped-speed sweep via `allowSceneActivation`); `bg_playground.png` re-rendered from new trail world + haze grade (usable; a tree-framed re-take would be nicer — old F13 take framed with trees, this one with skyline blocks) | ✅ done (verify in playtest) |
| **F16 game-feel pass** — **Hovl Magic effects pack** (`Assets/Hovl Studio/`, 34 mats code-converted Built-in→URP particles): looping Magic-circle pads under every answer card (`optionPadFxPrefab`), Star-hit burst on correct pick (`collectMagicFxPrefab`), Portal-blue floating at FINISH (`finishPortalPrefab`); trackside scenery de-citied (cars/van/traffic light → trees/flowers/bush; 100% park now); clouds higher/wider/5-puff, skyline pushed to 34+ lateral; HUD restyled nested: banner = star icon + navy "n/5" progress pill, danger meter = bar_bg/bar_fill + warning icon, feedback text on dark pop-in pill (`feedbackGroup`, brightened text colors); animations: new `Jump` trigger (RunningJump clip) fires on correct pick, finish = 2.2s victory beat (Dance + fireworks visible before Arrange loads) | ✅ done (verify in playtest) |
| **F17 playtest-feedback fixes** — **gray world fixed**: 86 ithappy model importers re-remapped (`SearchAndRemapMaterials` BasedOnMaterialName/Everywhere — embedded mats had `_BaseMap=NULL` gray; buildings/cars now full color); patrol menace beat (`_menaceTimer` 1.6s on wrong pick → patrol treats danger as 0.85 and visibly closes in; max leash 12.8→10m); danger meter hidden during briefing; briefing popup restyled (gold title pill overlapping Frame 1583, cream body card `Rectangle 356 (1)`, 5 SWBST color chips S/W/B/S/T, kit green `Buttons/Start.png` + dark-green ring, TMP label disabled — baked text) | ✅ done (verify in playtest) |
| **F18 SWBST color language** — `Constants/SwbstPalette.cs` (S=blue W=green B=red S=orange T=purple + Pastel/Deep/Hex helpers) applied everywhere: race gate pills (white text on element color), Arrange empty slots (pastel bg + deep-color bold label; filled/locked states unchanged), Summary reference list (TMP rich-text colored types), briefing chips match. **Endless Runner sample = reference only** — never import into this project (complete-project package: overwrites ProjectSettings, ships Ads/Analytics/IAP which are banned; study it in a separate throwaway project and port techniques) | ✅ done (verify in playtest) |
| **F19 Trash Dash ports + decontamination** — studied `C:\...\GAME\Endless Runner - Sample Game` from disk (reference only, keep it that way): ported ① progressive run speed (`GameRules.RaceAccelPerSecond/RaceAccelMaxBonus`, +0.5%/s cap +25%, anim+footsteps synced) ② center-lane coin lines between gates (`BuildCoinLines`, `CoinPickup.cs` spin+ripple-bob+fly-up, `sfx_coin` = Kenney handleCoins, pure juice — no scoring). **Removed Endless-Runner contamination from SummaRace2**: reverted manifest (it had gained com.unity.ads/analytics/purchasing!), deleted `Assets/MobileDependencyResolver` + `Assets/Resources/BillingMode.json` (Google VersionHandler self-resurrects — needed a second delete after domain reload; if it ever returns, delete with the editor closed) | ✅ done (verify in playtest) |
| **F20 Results treasure reveal** — chest (Gems and gold sprite, UIFloat) + `TreasureRow` at the bottom of Results; `ResultsController.RevealTreasure()` pops in 5 SWBST-colored letter chips after the stars (full color = first pick right, dimmed = missed; `sfx_coin` tick per chip; editor-direct fallback = all earned). Closes the Boot "stories are treasure" metaphor. Race music loop still missing (Kenney has only short jingles — needs a real loop, flag ④ stands) | ✅ done (verify in playtest) |
| **F21 golden story sparkle + natural run** (agent pass) — all Hovl FX retinted warm gold at spawn via `RaceController.TintFx` (StoryGold 1/0.85/0.45; startColor + colorOverLifetime, alphas kept); pads swapped Magic circle → **Healing circle** @0.45 scale (golden glow ring, no runes), finish Portal blue → **Portal yellow**, Star hit collect burst reads white-gold; RunnerAnimator Jump blends 0.15 in / 0.30 out; `PlayerRunner.LeanIntoLaneChange` yaws the model ≤12° into lane switches (LerpAngle 9/s, null-safe for grey-box) | ✅ done (verify in playtest) |
| **F22 StorySelect select-level redesign** — per owner's reference shots: Hill deleted, bright sky + generated `cloud_soft.png` clouds (UIFloat), gold banner title, kit `Level screen pannel` board; EASY = gold `Daily Reward pannel` card (hero art, green EASY chip, 3-star row from new `GameManager.GetBestStars`, glossy kit `Play` visual — whole card is the button); AVERAGE/HARD = dimmed `Frame 1583` cards (chip + big lock + Locked/hint, light text); Back = kit `Buttons/Back.png` (baked label, TMP child disabled); learner strings → `GameText` (StorySelectTitle/Difficulty*/Locked*) set by controller; stars = ResultsController silhouette trick | ✅ done (verify in playtest) |
| **F23 pro-look pass (Trash Dash art mined)** — `Plugins/TrashDash Art/` = Models+Textures+Materials+Default-theme prefabs copied from the Endless Runner reference project (37MB, metas kept so GUIDs resolve; third-party notice included; **art only — no scenes/scripts/shaders**; 61 mats converted Curved→URP): **modeled clouds** (`RaceController.cloudPrefabs` ← DefaultCloud01-03, sphere-puff fallback kept); their obstacles stay unused (textures were runtime theme-bound → render gray); generated tileable `trail_dirt.png` (`trailTexture` field → speckled ground); **Bloom** post-FX (`_Game/Art/RacePostFx.asset` volume in Race, threshold .9 intensity .55, camera renderPostProcessing on; both RP tiers have HDR — check 2GB-device frame rate in Phase J) | ✅ done (verify in playtest) |
| **F24 race music + MainMenu polish** — `music_race.ogg` = Trash Dash `STEMSMainTrackMono` (Unity sample license; loops from GO!, `StopMusic()` at finish so the victory sting owns Results); MainMenu restyled as Boot's interactive twin (bg_playground backdrop, crown + gold title lockup w/ float+pop, white Nunito tagline, coins/gems decor, enlarged TAP TO START w/ ring + pop). **Endless Runner experiment lives on branch `experiment/endless-runner-import`** (their full game runs there via Assets/Scenes/Main.unity; has ads/IAP packages — NEVER build/ship from it; main stays clean) | ✅ done (verify in playtest) |
| **F25 race course dressing** — all code-built in `RaceController` (no scene edits): wooden **start arch** at z=8 (posts+crossbar, gold START card, flags) run under at GO!; **SWBST bunting** (`BuildBunting`, 9 flags cycling the palette, shared mats) with rope strung over every checkpoint (parented to gate root → recycles/destroys with it); **checkered finish strip** (`BuildFinishStrip`, 2×12 tiles) on the trail at the finish line | ✅ done (verify in playtest) |
| **F26 Trash Dash road world** — cracked the gray-art mystery: their art is **vertex-colored + unlit** (no textures); all 60 `TrashDash Art/Materials` converted to `URP/Particles/Unlit` (multiplies vertex color) + models re-remapped → full Day-theme pastel color. Race gained **road mode**: `roadSegmentPrefabs` (Road01–04, 9m flat street slabs laid end to end) + `sideDressingPrefabs` (Suburbs houses/garage, auto-slid outward via renderer-bounds until clear of the street, blob-shadow renderers disabled, colliders stripped) via `BuildRoadWorld`/`SpawnDressing`; player/patrol/gates/coins/arch/finish extracted to `BuildPlayerPatrolAndGates()` shared by both modes; **empty road array = old dirt-trail park mode** (grey-box fallback intact). TODO later: regen `bg_playground` from road world for menu consistency | ✅ done (verify in playtest) |
| **F27 endless-runner look parity** — ⓐ **curved world**: `_Game/Art/Shaders/CurvedVertexColor.shader` (URP port of their CurvedCode.cginc clip-space bend; global `_CurveStrength`, their shipped **0.005**, set by RaceController in road mode / 0 in park); 40 TrashDash world mats on it (21 FX skipped); road-mode primitives via `WorldMat` factory; **alley corridor** (near urban walls clear 10.5, `farDressingPrefabs` houses/warehouses, `sidewalkPropPrefabs` cones/bins, `SlideClearOfStreet` helper); camera = their Main scene values (0,4,-3) 15° FOV 59/67. ⓑ **patrol = BitGem cop**: rig converted Generic→**Humanoid (valid!)** — old F12 flag was never tested; `_Game/Prefabs/PatrolCop` w/ PatrolAnimator+copAvatar (Run = PoliceRun clip). ⓒ **branding**: Boot + SceneLoader overlay on `Resources/UI/bg_logo_radial` (20 Logos pack), treasure decor deleted from Boot; `bg_playground` regenerated from the curved corridor | ✅ done (verify in playtest) |
| **F28 template logo lockup** — Boot + MainMenu title rebuilt as the 20-Logos "Title 1" composition with our name (pack PNGs have baked names → recreated): crown + stacked **SUMMA / RACE!** (Fredoka Display gold-gradient TMP + dark depth copy offset −9 = 3D extrusion, ±2.5° tilt) + 3 kit-star sparkles, UIFloat+PanelIntro on the lockup. To change wording/sizes: `LogoLockup` in both scenes | ✅ done (verify in playtest) |
| **F29 corridor sections fixed (track audit)** — key insight: TrashDash zone prefabs (UrbanBrickWalls/SuburbsHouse/etc.) **span BOTH street sides** (walls+wires+fences in one piece, 52–76m wide) → placed **centered at x=0, stacked along z by each piece's measured depth** (`sideDressingPrefabs` = 10-section mixed pool; `SlideClearOfStreet`/`farDressingPrefabs` deleted); Cartoon City skyline removed from road mode (uncurved mats detach from bent horizon; park mode keeps it); fog retuned 35/110 → **70/260** (was bleaching the corridor). Verified via 2-position sweep: reads like Trash Dash suburbs/alley | ✅ done (verify in playtest) |
| **F30 playtest readability fixes** — answer cards 1.2×0.66→**1.55×0.85** (font max 2.4), SWBST pill 2.6→3.0 wide, gold pads 0.45→**0.26** (one ring per lane, no overlap), camera −3→**−3.9 @17°** so the runner is framed (owner's race/loading screenshots were from a pre-F26 session — no street, old loading; current build differs) | ✅ done (verify in playtest) |
| **F31 item-display audit fixes** — ① uncurved gameplay visuals (answer cards/SWBST pill/pads under new per-gate `Items` group; finish gate+portal under `FinishGroup`) now follow the world bend via `CurveDip.cs` (dip = strength·d²·tan(FOV/2), static `Strength` set with road mode; dip→0 at pickup range so triggers stay honest) ② wrong pick destroys its glow ring (`OptionPickup.padFx` — no orphaned rings) ③ recycled gates clear coins within ±9m of the new z (no cards-in-coins overlap) | ✅ done (verify in playtest) |
| **EXP3 (branch experiment/endless-override-2) — Trash Dash IS the race**: their game untouched (one guarded line in TrackManager); `MainSummaRace.unity` copy + `EndlessRaceDirector` = SWBST gates replace coins/powerups, finite 5-gate run → Arrange; Reader routes to `SceneNames.RaceEndless`; original Main.unity still plays their full game. Getting here required repairing collateral damage from the "EXPERIMENT 2: full Endless Runner import" (`e4de43e`), which had overwritten project-wide config: the Default-Zones Addressables group lost the SummaRace zones (repaired in owner commit `ffaec91`), and `ProjectSettings/EditorBuildSettings.asset` had every SummaRace core scene dropped in favor of the sample's own list — `Arrange.unity` was restored first (Task 5, blocking FINISH→Arrange) and the remaining 11 (`Boot/MainMenu/StorySelect/Reader/Race/Summary/Results/NameEntry/SessionMap/TeacherMenu/Settings`) restored here (Task 6, guids verified against each scene's `.meta`), so `SceneLoader`/`SceneManager.LoadScene` can resolve the full game outside this branch's direct-editor scenes too. Full loop verified live: Boot→MainMenu→StorySelect→Reader→MainSummaRace(5 gates, mixed correct/wrong)→FINISH→Arrange→Summary→Results, with Results stars/treasure gems matching the exact first-pick mix produced during the run and no Trash Dash music bleeding past FINISH. Branch never ships (ads/IAP present). | ✅ done (verify in playtest) |
| **F32 Reader polish pass (scene-by-scene QA)** — prototype parity (screens 4→5): story card hides itself during the question so the question is its own bright page (scrim dropped to transparent), pages progress bar added to the top HUD row (`ReaderController.progressFill`); long answer options **wrap to two lines instead of shrinking** (s01_hard has a 58-char option that was rendering tiny next to the others); juice: option fan-in stagger, punch-scale on the correct answer, feedback pop-in, progress-bar sweep, FeedbackText → autosize 28–40 + wrap. **`Core/Praise.cs`** = shuffle-bag praise picker replacing the four hardcoded "Great job!" sites (Reader/Race/Arrange/Results); pools live in `GameText` (`PraiseGeneric` ×16, `PraiseByElement`, `ArrangePerfectPool`, `PraiseByStars` widened) — process praise not ability praise; race names a SWBST element every 3rd pick starting at a **random offset** (fixed start would name the same element every 5-gate story). Deliberately no sound on question-appear (OnNext's SfxClick already covers it; `PanelIntro` notes sfx_pop was removed from blanket panel pops). **Still open in Reader:** Back button (design call), `A./B./C.` option prefixes, narration replay without double-toggling VOICE. Tween timings unverified — motion only runs in Play mode. | ✅ done (verify in playtest) |
| **NEXT (office session):** ① owner full-loop playtest (= "M1 polished" gate) ② narration voice pick — A/B samples committed in `/NarrationSamples` (Rosa-PH current, Ana=child, Jenny/Aria US adult; after pick: regenerate s01 clips + delete folder) ③ "GUI - Basic Button Pack" never imported — re-import if wanted ④ race music loop still missing ⑤ teacher avatar real art + race pickup icons (mockup 17) ⑥ then Phase G: 29 stories + SessionMap (`Level screen.png` template) | ⬜ next |
| G 29 more stories + SessionMap · H asset pass · I research features (PIN/logging/export) · J device builds | ⬜ later |

Post-MVP scenes NameEntry/SessionMap/TeacherMenu/Settings exist as named scenes but are empty/placeholder by design. SampleScene deleted (GDD D18 done).

**Known flags:**
- `s01_easy.json` page-split/questions/distractors were AI-authored — the researcher's source doc (`Documentation/STORIES FOR SESSION 1-10 ….docx`) gives Day 1 **EASY** only a passage + main idea, while Day 1 AVERAGE/HARD get the full `5 PAGES:` + `Processing Question` treatment. Sign-off before the study build is still the right call (GDD D6), but as a formality: the authored questions follow the researcher's own pattern faithfully. **Do not "fix" them to be comprehension-shaped** — an earlier pass (2026-07-21) argued that and was wrong. The researcher's own processing questions are deliberately SWBST-shaped ("Who is the main character?" = Somebody, "What did Duncan want to do?" = Wanted, "What problem did the crayons have?" = But, …). The Reader **pre-teaches** the five slots with the text on screen; the Race asks the same five with the text gone. That is the support-removal ladder working — Read = scaffold → Race = unsupported recall → Arrange = sequence → Summary = produce — not redundancy. Same pattern applies to the Phase G stories. The source doc also writes options as `A. / B. / C.`, which is the case for adding letter prefixes in the Reader.
- `Resources/Stories/Art/s01_easy.png` is a **TEMP hero image** cropped from Mockups/3.png — replace when the researcher locks the 30-image style (Phase G). No image-gen provider keys are configured in MCP (`generate_image` needs fal.ai/OpenRouter key if wanted).
- ~~BitGem `cop.fbx` external-material warnings~~ **fixed** (InPrefab import + URP-converted `cop_blue.mat`); note cop is a **Generic** rig — it cannot play the Mixamo Humanoid clips, so the patrol uses Ty instead. `Aj.fbx` renders invisible (mesh offset?) — parked, not used.
- Narration: **s01_easy generated** (edge-tts `en-PH-RosaNeural --rate=-10%`, mp3s in `Resources/Stories/Narration/`). Reader auto-plays per page; VOICE ON/OFF button persists via `PrefKeys.NarrationOn`; `AudioManager.PlayNarration(path)`. For Phase G stories: write page text to a temp file and run `python -m edge_tts --voice en-PH-RosaNeural --rate=-10% -f <file> --write-media <id>_pN.mp3` (embedded quotes break inline `--text` in PS 5.1). Runtime TTS stays forbidden.
- Race world-space labels (pickups, gates, FINISH) still use legacy 3D `TextMesh` — restyle during the race reskin (F4), not part of the TMP UI migration.
- Design references inside Hyper_Casual_UI `Sprites/GameUI/`: `Victory Pannel.png` (Results screen, applied), `Level screen.png` (numbered circle grid = template for Phase G SessionMap), `Main Menu.png` (glossy icon pills). The pack's demo scenes are baked showcase sprites, not prefabs; several kit sprites ship with broken import settings (not Sprite type / Multiple mode with no rects) — fix importer before use.

## Architecture (implemented — follow these patterns)

- **Singletons** (`_Game/Scripts/Core/`): `Bootstrapper` (Boot scene) creates `[Core]` GameObject with `GameManager`, `AudioManager`, `SaveManager`, `SceneLoader` (DontDestroyOnLoad). `EventBus` is static pub/sub; event payloads in `Core/GameEvents.cs` (catalog in TDD §8).
- **Features never call each other** — raise EventBus events + read `GameManager` state. Scene flow only via `SceneLoader.Instance.Load(SceneNames.X)`.
- **Constants only** (`_Game/Scripts/Constants/`): `SceneNames`, `AudioKeys`, `PrefKeys`, `GameRules` (all tuning numbers), `GameText` (all learner-facing strings). Never hard-code a scene name, audio key, gameplay number, or UI string.
- **Content pipeline** (`_Game/Scripts/Data/`): `StoryLoader.Load("s01_easy")` reads `Resources/Stories/<id>.json` → validated `StoryData`. Add a story = add a JSON, zero code.
- **Audio**: clips live in `Assets/_Game/Resources/Audio/` named exactly per `AudioKeys` (e.g. `sfx_correct.ogg`); `AudioManager.Instance.PlaySfx(AudioKeys.SfxCorrect)`. Swap a sound = replace the file. Raw Kenney packs in `Assets/Audio/Kenney/` are the source library.
- **Every scene controller** null-checks `GameManager.Instance`/`CurrentStory` and falls back to `StoryLoader.Load("s01_easy")` so any scene can be played directly in-editor (TDD §13).
- **Naming**: PascalCase scripts/scenes/methods, `_camelCase` privates, `[SerializeField] private` over public, snake_case asset files, namespaces `SummaRace.<Layer>` matching folders.
- **Third-party packs go in `Assets/Plugins/`** (13 packs there now). Design reference for UI skin: `Plugins/Hyper_Casual_UI/Scenes/Demo_Game_UI`. Fonts: `Assets/Art/Fonts/` Fredoka (headings) + Nunito (body) — make TMP assets in Phase F. Characters + Mixamo clips (all Humanoid): `Assets/Art/Characters/`.

## Workflow rules (owner's standing instructions)

- **Professional quality at every stage**: grey-box = placeholder *visuals* only; logic must always be correct, complete, sound-wired, dead-end-free. Placeholder screens allowed only as temporary "never a dead end" stubs.
- **Commit + push after every verified milestone** (small commits). Owner playtests in the Editor before a milestone counts as done.
- Grey-box first, one story before thirty, GDD wins design arguments, docs win over improvisation.

## Working with the Unity Editor (MCP)

This project has **MCP For Unity** (`com.coplaydev.unity-mcp`) installed. When a Unity Editor instance is connected, prefer the `mcp__UnityMCP__*` tools over editing serialized assets by hand:

- **Read editor/project state via MCP resources**, then mutate via tools — check state before changing it.
- Scenes/objects: `manage_scene`, `manage_gameobject`, etc. — raw `.unity`/`.prefab` YAML edits break GUID/fileID references. Complex scene builds work well via `execute_code` (C#6-ish subset; use `UnityEngine.Object.DestroyImmediate`, wire `[SerializeField]` fields via `SerializedObject`).
- C#: write files, then refresh with compile and verify the type exists in `Assembly-CSharp` before building scenes that reference it.
- Do **not** run Unity CLI `-batchmode` while the Editor is open — it locks the project.
- **Gotchas learned:** (1) If a new `.cs` validates clean but its type never appears in `Assembly-CSharp` (compile time ~1ms, file missing from CompilationPipeline sources), Unity's incremental compiler has stale tracking → delete the `.cs`+`.meta`, refresh, re-add the file fresh. (2) Play mode pauses without Editor focus (Run In Background off) — the MCP bridge stalls; have the owner click Unity/press Play. (3) Asset-store imports land in `Assets/` root — move to `Plugins/` via `AssetDatabase.MoveAsset` (or move file+`.meta` together on disk), never a bare file move.

## Code layout & assemblies

No `.asmdef` files: runtime → `Assembly-CSharp`, editor-only (`Editor/` folders) → `Assembly-CSharp-Editor`. Introduce asmdefs before adding test assemblies or if compile times grow.

## Rendering

Two URP tiers (Project Settings → Quality/Graphics): `Assets/Settings/PC_RPAsset` + `PC_Renderer`, `Mobile_RPAsset` + `Mobile_Renderer`. Apply renderer/shader/post changes to **both** (or deliberately tier-gate). Device floor: 2GB Android 8, 30fps in race, APK ≤ 300MB. Android: IL2CPP, ARM64, min API 26, portrait locked.

## Input

New Input System only (`Assets/InputSystem_Actions.inputactions`). UI needs `InputSystemUIInputModule` on the EventSystem. Race lane-switching polls `Keyboard.current`/`Touchscreen.current` (see `PlayerRunner`).
