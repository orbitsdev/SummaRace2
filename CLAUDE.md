# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

SummaRace is an **offline Android reading game** (Unity 6 / URP 17.4.0, portrait, new Input System) teaching Grade-4 learners to summarize stories via the **SWBST framework** (Somebody·Wanted·But·So·Then). It is a **college thesis instrument** — 40 learners will use it across 10 sessions and be compared to a control group.

**Read the docs in `Documentation/` before making design decisions.** There are sixteen; read them
in this order and do not restate one inside another:

*The two that claim to be current:*
- `SummaRace_Owner_Handover.md` — **the front door.** State, ordered critical path, the owner's open decisions, and §4d "where the documents disagree and which one is right"
- `SummaRace_Finalization_Plan.md` — the remaining work with effort (**the live plan**)

*Dated snapshots — each is true as of its own header, triaged in Handover §4c/§4d:*
- `SummaRace_Build_And_Release.md` — toolchain → preflight → Addressables → APK → sideload → smoke test
- `SummaRace_Device_Budget.md` — **the measured authority** on RAM / APK weight / render pipeline. Where any other doc quotes a size, this one wins
- `SummaRace_Study_Operations_Runbook.md` — the classroom side (PIN, participant codes, sessions, export, recovery)
- `SummaRace_Data_Dictionary.md` — every field in the exported `.jsonl` (check its schema number against `SessionLogService.SchemaVersion` before trusting it)
- `SummaRace_Content_QA_Report.md` · `SummaRace_Story_Alignment_Audit.md` — the 30 stories measured against the geometry they render into, and page↔SWBST-slot alignment
- `SummaRace_Readability_And_Accessibility_Audit.md` · `SummaRace_Audio_Audit.md` — type size in arcmin, WCAG contrast, tap targets; every interactive moment's sound
- `SummaRace_Pro_Review.md` — an outside pre-ship read; argues against some decisions recorded here. Its ranked list is superseded by Handover §2 (see its own header note)
- `/missingassets/` (repo root, NOT `Documentation/`) — the art-generation working folder: `README.md` = start here, `CHECKLIST.md` = the 27 filenames, `01_hero_images_and_shopping_list.md` §2 = the 27 ready-to-paste prompts, `dropin/` + `install.ps1` = save-and-install. Moved out of `Documentation/` on 2026-08-19; stubs left behind point here

*Design intent, not implementation:*
- `SummaRace_Project_Brief.md` — orientation (read first if you are new)
- `SummaRace_Final_GDD.docx` — design bible; all decisions D1–D18 are LOCKED
- `SummaRace_Technical_Design_Document.md` — original implementation spec (the build has deliberately diverged)
- `SummaRace_Build_Guide.md`, `SummaRace_Asset_Requirements_List.md` — **historical**; phases A–J are done and the bill of materials is superseded by the Shopping List
- `AssetGeneration/README.md` — ten world recipes, weather/mood, collectible skins (§1–3) + assets still to be produced (§4)
- `Documentation/Mockups/1..27.png` — validated prototype screens (visual target). Live demo: https://tdrx44.csb.app/

**Non-negotiables:** 100% offline (no networking/ads/analytics), never punish the learner (wrong answers never block; "caught" is friendly, never game-over), content is JSON data never code, all 30 stories flow through one reusable engine.

## Build state (updated 2026-08-06, HEAD `63e1be3`)

**Content is verified green through the real runtime loader** (not by reading the JSON): all
30 stories load through `StoryLoader` with zero validation failures, all 150 narration paths
resolve to actual `AudioClip` assets, all 30 `heroImage` PNGs exist, every story has 5 pages
and 5 elements in S-W-B-S-T order with 3 distinct options and an in-range `correctIndex`,
the longest race-card string is 53 chars (re-measured 2026-08-21: **52 chars**, `s01_easy` SO), and the 10 worlds are one per session. Don't
re-derive this by hand — re-run the check if content changes.

**The build wins over the documents.** The GDD is intent; the game has deliberately diverged for playability — the biggest divergence is that the race is now the Trash Dash endless runner (`MainSummaRace`), not the doc's park/trail race. Consult the docs for design questions the code hasn't already answered.

> **⚠️ Reading the table below.** Rows **F1–F31 describe the legacy `Assets/_Game/Scenes/Race.unity`
> race** (`RaceController`, park/trail, dirt lanes, Cartoon City skyline, danger meter). That scene
> **no longer ships anything** — it is build index 6 with **zero call sites**. From **EXP3** onward
> the race is Trash Dash's `Assets/Scenes/MainSummaRace.unity` driven by
> `Features/Race/Endless/EndlessRaceDirector.cs`. Do not act on an F1–F31 detail without checking
> which race it belongs to. Rows that a later row replaced are marked **superseded** in place.

| Phase | Status |
|---|---|
| B skeleton, C core systems, D content pipeline | ✅ done |
| **E vertical slice (M1)** — full loop Boot→MainMenu→StorySelect→Reader→Race→Arrange→Summary→Results, grey-box, sounds wired | ✅ **code-complete**; full-loop playtest by owner pending |
| **F1 typography** — all UI on TextMeshPro; TMP assets in `Art/Fonts/TMP/` (Fredoka-SemiBold = headings/buttons, Nunito-Regular = body + TMP default, Nunito-Bold = feedback); Summary uses TMP_InputField; Results stars are Hyper_Casual_UI sprite Images | ✅ done (verify in playtest) |
| **F2 UI skin** — Hyper_Casual_UI kit applied: 9-sliced pill buttons + Rectangle 356 panels everywhere, sky/curved-hill backdrops (generated `_Game/Art/UI/hill_arc.png`), race banner on toggled BannerBG pill | ✅ done (verify in playtest) |
| **F3 juice + bordered panels** — major cards on the kit's bordered popup sprites (gold `Daily Reward pannel`, teal `Frame 1583`); `SummaRace.UI.ButtonSquash` on every button, `PanelIntro` pop-in on cards/popups; PrimeTween PunchScale on stars + race feedback | ✅ done (verify in playtest) |
| **F4 race reskin** — `_Game/Prefabs/PlayerCharacter` (Ch46 kid) + `PatrolCharacter` (Ty, ZombieRun) spawn via `RaceController.playerModelPrefab/patrolModelPrefab` (grey-box fallback if unwired); AnimatorControllers in `_Game/Animation/` (Running bool, Stumble/Dance triggers); grass track w/ blue-yellow-green lanes | ✅ done (verify in playtest) |
| **F4b world dressing** — Supercyan forest props (URP-converted) line the race track via `RaceController.sceneryPrefabs`/`BuildScenery`, deterministic scatter, 2x scale | ✅ done (verify in playtest) |
| **F5 hero + world labels** — StorySelect easy card shows hero image + title from story JSON (missing art → title-only fallback); race world text is 3D TMP (Fredoka) on dark backing quads | ✅ done (verify in playtest) |
| **F6 eye-candy** — gradient backgrounds all UI scenes (`v_gradient(_soft).png`), gold-gradient floating title w/ `Fredoka…Display.mat` underlay shadow, `RaceChaseCamera` (lane follow + boost FOV kick via `RaceController.SpeedMultiplier`) | ✅ done (verify in playtest) |
| **F7 playtest fixes** — KI natural run + animator-speed sync; loading overlay = gradient + gold tip card; CFXR FX in race (collect/wrong/caught POW/finish fireworks); dark border rings on CTA buttons; teacher avatar + speech bubble on Arrange/Summary (TEMP crop from mockup 21); pickups bob | ✅ done (verify in playtest) |
| **F8 race environment** — Supercyan skybox (shader restored — do NOT batch-convert `Skybox/*` mats) + linear fog; SimplePoly fence lines both sides; unlit puffy sphere clouds; 14 scenery slots incl. benches/lights/flowers/ithappy animals; EnergyShell glow on correct pickup; 72 SimplePoly+NaturePack mats URP-converted | ✅ done (verify in playtest) |
| **F9 runner feel** — subway-runner camera (0,3.3,-5.4 @13°, FOV 64/74); SmoothCameraShaker RockImpact on caught; KI LeanLeft/Right on lane switch (PlayerRunner→`RaceController.OnLaneSwitched`); +6 Kenney sounds (press tap, panel pop, transition chime, 3 speed-synced grass footsteps) | ✅ done (verify in playtest) |
| **F11 race readability** — world text = 9-sliced kit-sprite cards (`RaceController.BuildWorldCard`, SpriteRenderer): white answer cards ARE the pickups (invisible trigger cubes), yellow SWBST pill + FINISH card; gold-tint highlight; collect = fly-up pop; recycle clamped (no gate stacking); Boot splash styled; hills on 4 scenes | ✅ done |
| **F12 patrol/city/icons** — ⚠️ **the patrol geometry in this row is dead twice over**: it describes the *legacy* race, and the endless race's chase was rebuilt in F34 (camera-derived band), again in F39 (appear-on-bump, no danger creep) and again in F47ⓑ (live renderer bounds). **Never restore 1.8–12.8m** — F34 measured that it would park the cop *in front of* the runner. Patrol range fixed 1.8–12.8m so it *looms into camera view* at high danger (was always behind the camera); sun aimed down-road; lock icons (`Hyper_Casual_UI/Sprites/Icons/lock.png`) on locked cards; Layer Lab trophy on Results; **Cartoon City FREE** in `Plugins/ithappy/` → `BuildSkyline()` buildings + palms/traffic lights/cars in scenery; 79 model importers fixed | ✅ done |
| **F13 game backdrop** — `_Game/Art/UI/bg_playground.png` = the 3D world rendered + soft-blurred, used as Reader/Arrange/Summary background (mockup-4 style, per-scene tints; Hill disabled there) | ✅ done |
| **F14 boot splash v2 + race pacing** — Boot rebuilt: `bg_playground` backdrop, 150pt gold title + `GameText.BootTagline` + treasure decor (Gems and gold pack chest/coins/gems, UIFloat bob + PanelIntro pop), `GameRules.SplashSeconds` (2s); `Bootstrapper` sets tagline/loading text from GameText; s01 `checkpointSpacing` 30→45 (~47s run, ~8s reading per gate). New packs moved to `Plugins/`: `20 Logos PSD` (title PNGs have baked names — PSD edit needed for a SummaRace logo; fonts + sunburst bg usable), `Game GUI Vol1` (4 icon-button themes; Playful fits — candidates: VOICE ON/OFF Sound icons, Pause in race), `Gems and gold` (coins/diamonds/chests; 512 PNGs; importers default to Texture — set Sprite before use) | ✅ done (verify in playtest) |
| **F15 adventure theme pass** — race lanes → natural dirt trail (matte tan bed + packed-earth edges + soft cream dashed guides, shared mats), `RaceController.tracksideObstaclePrefabs` (5 SimpleNaturePack rocks/bush/mushroom/stump @2.2x on trail shoulder, colliders stripped, skips checkpoint zones); Boot: kit crown icon over title (royal-logo look) + splash progress bar (`Bootstrapper.splashFill`); SceneLoader overlay got real async progress bar (generated `Resources/UI/bar_bg/bar_fill` pill sprites, capped-speed sweep via `allowSceneActivation`); `bg_playground.png` re-rendered from new trail world + haze grade (usable; a tree-framed re-take would be nicer — old F13 take framed with trees, this one with skyline blocks) | ✅ done (verify in playtest) |
| **F16 game-feel pass** — **Hovl Magic effects pack** (`Assets/Hovl Studio/`, 34 mats code-converted Built-in→URP particles): looping Magic-circle pads under every answer card (`optionPadFxPrefab`), Star-hit burst on correct pick (`collectMagicFxPrefab`), Portal-blue floating at FINISH (`finishPortalPrefab`); trackside scenery de-citied (cars/van/traffic light → trees/flowers/bush; 100% park now); clouds higher/wider/5-puff, skyline pushed to 34+ lateral; HUD restyled nested: banner = star icon + navy "n/5" progress pill, danger meter = bar_bg/bar_fill + warning icon, feedback text on dark pop-in pill (`feedbackGroup`, brightened text colors); animations: new `Jump` trigger (RunningJump clip) fires on correct pick, finish = 2.2s victory beat (Dance + fireworks visible before Arrange loads) | ✅ done (verify in playtest) |
| **F17 playtest-feedback fixes** — **gray world fixed**: 86 ithappy model importers re-remapped (`SearchAndRemapMaterials` BasedOnMaterialName/Everywhere — embedded mats had `_BaseMap=NULL` gray; buildings/cars now full color); patrol menace beat (`_menaceTimer` 1.6s on wrong pick → patrol treats danger as 0.85 and visibly closes in; max leash 12.8→10m); danger meter hidden during briefing; briefing popup restyled (gold title pill overlapping Frame 1583, cream body card `Rectangle 356 (1)`, 5 SWBST color chips S/W/B/S/T, kit green `Buttons/Start.png` + dark-green ring, TMP label disabled — baked text) | ✅ done (verify in playtest) |
| **F18 SWBST color language** — `Constants/SwbstPalette.cs` (S=blue W=green B=red S=orange T=purple + Pastel/Deep/Hex helpers) applied everywhere: race gate pills (white text on element color), Arrange empty slots (pastel bg + deep-color bold label; filled/locked states unchanged), Summary reference list (TMP rich-text colored types), briefing chips match. **Endless Runner sample = reference only** — never import into this project (complete-project package: overwrites ProjectSettings, ships Ads/Analytics/IAP which are banned; study it in a separate throwaway project and port techniques) | ✅ done (verify in playtest) |
| **F19 Trash Dash ports + decontamination** — studied `C:\...\GAME\Endless Runner - Sample Game` from disk (reference only, keep it that way): ported ① progressive run speed (`GameRules.RaceAccelPerSecond/RaceAccelMaxBonus`, +0.5%/s cap +25%, anim+footsteps synced) ② center-lane coin lines between gates (`BuildCoinLines`, `CoinPickup.cs` spin+ripple-bob+fly-up, `sfx_coin` = Kenney handleCoins, pure juice — no scoring). **Removed Endless-Runner contamination from SummaRace2**: reverted manifest (it had gained com.unity.ads/analytics/purchasing!), deleted `Assets/MobileDependencyResolver` + `Assets/Resources/BillingMode.json` (Google VersionHandler self-resurrects — needed a second delete after domain reload; if it ever returns, delete with the editor closed) | ✅ done (verify in playtest) |
| **F20 Results treasure reveal** — chest (Gems and gold sprite, UIFloat) + `TreasureRow` at the bottom of Results; `ResultsController.RevealTreasure()` pops in 5 SWBST-colored letter chips after the stars (full color = first pick right, dimmed = missed; `sfx_coin` tick per chip; editor-direct fallback = all earned). Closes the Boot "stories are treasure" metaphor. Race music loop still missing (Kenney has only short jingles — needs a real loop, flag ④ stands) | ✅ done (verify in playtest) |
| **F21 golden story sparkle + natural run** (agent pass) — all Hovl FX retinted warm gold at spawn via `RaceController.TintFx` (StoryGold 1/0.85/0.45; startColor + colorOverLifetime, alphas kept); pads swapped Magic circle → **Healing circle** @0.45 scale (golden glow ring, no runes), finish Portal blue → **Portal yellow**, Star hit collect burst reads white-gold; RunnerAnimator Jump blends 0.15 in / 0.30 out; `PlayerRunner.LeanIntoLaneChange` yaws the model ≤12° into lane switches (LerpAngle 9/s, null-safe for grey-box) | ✅ done (verify in playtest) |
| **F22 StorySelect select-level redesign** — per owner's reference shots: Hill deleted, bright sky + generated `cloud_soft.png` clouds (UIFloat), gold banner title, kit `Level screen pannel` board; EASY = gold `Daily Reward pannel` card (hero art, green EASY chip, 3-star row from new `GameManager.GetBestStars`, glossy kit `Play` visual — whole card is the button); AVERAGE/HARD = dimmed `Frame 1583` cards (chip + big lock + Locked/hint, light text); Back = kit `Buttons/Back.png` (baked label, TMP child disabled); learner strings → `GameText` (StorySelectTitle/Difficulty*/Locked*) set by controller; stars = ResultsController silhouette trick | ✅ done (verify in playtest) |
| **F23 pro-look pass (Trash Dash art mined)** — `Plugins/TrashDash Art/` = Models+Textures+Materials+Default-theme prefabs copied from the Endless Runner reference project (37MB, metas kept so GUIDs resolve; third-party notice included; **art only — no scenes/scripts/shaders**; 61 mats converted Curved→URP): **modeled clouds** (`RaceController.cloudPrefabs` ← DefaultCloud01-03, sphere-puff fallback kept); their obstacles stay unused (textures were runtime theme-bound → render gray); generated tileable `trail_dirt.png` (`trailTexture` field → speckled ground); ~~**Bloom** post-FX (`_Game/Art/RacePostFx.asset` volume in Race, threshold .9 intensity .55, camera renderPostProcessing on)~~ — ⚠️ **false for the shipping race, and false on disk now**: the profile's `components:` holds one **null** entry (no Bloom override survives) and the only camera with `m_RenderPostProcessing: 1` anywhere in the build list is in the dead legacy `Race.unity`. `MainSummaRace` has none. Do not offer Bloom as a frame-rate lever — there is nothing to turn off | ✅ done (verify in playtest) |
| **F24 race music + MainMenu polish** — `music_race.ogg` = Trash Dash `STEMSMainTrackMono` (Unity sample license; loops from GO!, `StopMusic()` at finish so the victory sting owns Results); MainMenu restyled as Boot's interactive twin (bg_playground backdrop, crown + gold title lockup w/ float+pop, white Nunito tagline, coins/gems decor, enlarged TAP TO START w/ ring + pop). **Endless Runner experiment lives on branch `experiment/endless-runner-import`** (their full game runs there via Assets/Scenes/Main.unity; has ads/IAP packages — NEVER build/ship from it; main stays clean) | ✅ done (verify in playtest) |
| **F25 race course dressing** — all code-built in `RaceController` (no scene edits): wooden **start arch** at z=8 (posts+crossbar, gold START card, flags) run under at GO!; **SWBST bunting** (`BuildBunting`, 9 flags cycling the palette, shared mats) with rope strung over every checkpoint (parented to gate root → recycles/destroys with it); **checkered finish strip** (`BuildFinishStrip`, 2×12 tiles) on the trail at the finish line | ✅ done (verify in playtest) |
| **F26 Trash Dash road world** — cracked the gray-art mystery: their art is **vertex-colored + unlit** (no textures); all 60 `TrashDash Art/Materials` converted to `URP/Particles/Unlit` (multiplies vertex color) + models re-remapped → full Day-theme pastel color. Race gained **road mode**: `roadSegmentPrefabs` (Road01–04, 9m flat street slabs laid end to end) + `sideDressingPrefabs` (Suburbs houses/garage, auto-slid outward via renderer-bounds until clear of the street, blob-shadow renderers disabled, colliders stripped) via `BuildRoadWorld`/`SpawnDressing`; player/patrol/gates/coins/arch/finish extracted to `BuildPlayerPatrolAndGates()` shared by both modes; **empty road array = old dirt-trail park mode** (grey-box fallback intact). TODO later: regen `bg_playground` from road world for menu consistency | ✅ done (verify in playtest) |
| **F27 endless-runner look parity** — ⓐ **curved world**: `_Game/Art/Shaders/CurvedVertexColor.shader` (URP port of their CurvedCode.cginc clip-space bend; global `_CurveStrength`, their shipped **0.005**, set by RaceController in road mode / 0 in park); 40 TrashDash world mats on it (21 FX skipped); road-mode primitives via `WorldMat` factory; **alley corridor** (near urban walls clear 10.5, `farDressingPrefabs` houses/warehouses, `sidewalkPropPrefabs` cones/bins, `SlideClearOfStreet` helper); camera = their Main scene values (0,4,-3) 15° FOV 59/67. ⓑ **patrol = BitGem cop**: rig converted Generic→**Humanoid (valid!)** — old F12 flag was never tested; `_Game/Prefabs/PatrolCop` w/ PatrolAnimator+copAvatar (Run = PoliceRun clip). ⓒ **branding**: Boot + SceneLoader overlay on `Resources/UI/bg_logo_radial` (20 Logos pack), treasure decor deleted from Boot; `bg_playground` regenerated from the curved corridor | ✅ done (verify in playtest) |
| **F28 template logo lockup** — Boot + MainMenu title rebuilt as the 20-Logos "Title 1" composition with our name (pack PNGs have baked names → recreated): crown + stacked **SUMMA / RACE!** (Fredoka Display gold-gradient TMP + dark depth copy offset −9 = 3D extrusion, ±2.5° tilt) + 3 kit-star sparkles, UIFloat+PanelIntro on the lockup. To change wording/sizes: `LogoLockup` in both scenes | ✅ done (verify in playtest) |
| **F29 corridor sections fixed (track audit)** — key insight: TrashDash zone prefabs (UrbanBrickWalls/SuburbsHouse/etc.) **span BOTH street sides** (walls+wires+fences in one piece, 52–76m wide) → placed **centered at x=0, stacked along z by each piece's measured depth** (`sideDressingPrefabs` = 10-section mixed pool; `SlideClearOfStreet`/`farDressingPrefabs` deleted); Cartoon City skyline removed from road mode (uncurved mats detach from bent horizon; park mode keeps it); fog retuned 35/110 → **70/260** (was bleaching the corridor). Verified via 2-position sweep: reads like Trash Dash suburbs/alley | ✅ done (verify in playtest) |
| **F30 playtest readability fixes** — answer cards 1.2×0.66→**1.55×0.85** (font max 2.4), SWBST pill 2.6→3.0 wide, gold pads 0.45→**0.26** (one ring per lane, no overlap), camera −3→**−3.9 @17°** so the runner is framed (owner's race/loading screenshots were from a pre-F26 session — no street, old loading; current build differs) | ✅ done (verify in playtest) |
| **F31 item-display audit fixes** — ① uncurved gameplay visuals (answer cards/SWBST pill/pads under new per-gate `Items` group; finish gate+portal under `FinishGroup`) now follow the world bend via `CurveDip.cs` (dip = strength·d²·tan(FOV/2), static `Strength` set with road mode; dip→0 at pickup range so triggers stay honest) ② wrong pick destroys its glow ring (`OptionPickup.padFx` — no orphaned rings) ③ recycled gates clear coins within ±9m of the new z (no cards-in-coins overlap) | ✅ done (verify in playtest) |
| **EXP3 (branch experiment/endless-override-2) — Trash Dash IS the race**: their game untouched (one guarded line in TrackManager); `MainSummaRace.unity` copy + `EndlessRaceDirector` = SWBST gates replace coins/powerups, finite 5-gate run → Arrange; Reader routes to `SceneNames.RaceEndless`; original Main.unity still plays their full game. Getting here required repairing collateral damage from the "EXPERIMENT 2: full Endless Runner import" (`e4de43e`), which had overwritten project-wide config: the Default-Zones Addressables group lost the SummaRace zones (repaired in owner commit `ffaec91`), and `ProjectSettings/EditorBuildSettings.asset` had every SummaRace core scene dropped in favor of the sample's own list — `Arrange.unity` was restored first (Task 5, blocking FINISH→Arrange) and the remaining 11 (`Boot/MainMenu/StorySelect/Reader/Race/Summary/Results/NameEntry/SessionMap/TeacherMenu` + the then-empty `Settings`, which **F43 later cut**) restored here (Task 6, guids verified against each scene's `.meta`), so `SceneLoader`/`SceneManager.LoadScene` can resolve the full game outside this branch's direct-editor scenes too. **Build Settings today is 12 scenes** (verified in `ProjectSettings/EditorBuildSettings.asset`): Boot **index 0**, MainSummaRace 1, Arrange 2, MainMenu 3, StorySelect 4, Reader 5, legacy **Race 6 (zero call sites)**, Summary 7, Results 8, NameEntry 9, SessionMap 10, TeacherMenu 11 — no `Settings`, none of theirs. Full loop verified live: Boot→MainMenu→StorySelect→Reader→MainSummaRace(5 gates, mixed correct/wrong)→FINISH→Arrange→Summary→Results, with Results stars/treasure gems matching the exact first-pick mix produced during the run and no Trash Dash music bleeding past FINISH. Branch never ships (ads/IAP present). | ✅ done (verify in playtest) |
| **F32 Reader polish pass (scene-by-scene QA)** — prototype parity (screens 4→5): story card hides itself during the question so the question is its own bright page (scrim dropped to transparent), pages progress bar added to the top HUD row (`ReaderController.progressFill`); long answer options **wrap to two lines instead of shrinking** (s01_hard has a 58-char option that was rendering tiny next to the others); juice: option fan-in stagger, punch-scale on the correct answer, feedback pop-in, progress-bar sweep, FeedbackText → autosize 28–40 + wrap. **`Core/Praise.cs`** = shuffle-bag praise picker replacing the four hardcoded "Great job!" sites (Reader/Race/Arrange/Results); pools live in `GameText` (`PraiseGeneric` ×16, `PraiseByElement`, `ArrangePerfectPool`, `PraiseByStars` widened) — process praise not ability praise; race names a SWBST element every 3rd pick starting at a **random offset** (fixed start would name the same element every 5-gate story). Deliberately no sound on question-appear (OnNext's SfxClick already covers it; `PanelIntro` notes sfx_pop was removed from blanket panel pops). **Still open in Reader:** Back button (design call), `A./B./C.` option prefixes, narration replay without double-toggling VOICE. Tween timings unverified — motion only runs in Play mode. | ✅ done (verify in playtest) |
| **F33 Reader→Race handoff + editor-direct dead-end fix** — ① **`SceneLoader.Go(scene)`** (static): overlay load when booted normally, plain `SceneManager.LoadScene` otherwise. Every feature previously exited via `if (SceneLoader.Instance != null) Instance.Load(...)` **with no else**, so playing any scene directly (Boot never runs → no `[Core]`) made its exit button do nothing silently — no exception, looked like a broken button. Only `EndlessRaceDirector` had the fallback. All 12 call sites converted incl. `GameManager`'s "never a dead end" story-load failure path (would have NRE'd). TDD §13 now actually holds. ② **endless-race mission briefing** — the last Reader question used to cut straight into a moving world; `EndlessRaceDirector.BuildBriefing()` (code-built, their scene stays free of our prefabs) = scrim + "Your Mission" card naming the story + 5 SWBST chips in gate colours + START! → `CountdownRoutine()` 3-2-1-GO! on the HUD banner → world released. `Update()` **re-asserts `TrackManager.StopMove()` every frame** until the countdown ends (stopping once loses: their `StartGame→WaitToStart` turns `isMoving` back on later); scrim also masks the ~1.25s Trash Dash boot. ③ Reader options carry **`A. / B. / C.`** (`GameText.OptionLetters`), left-aligned with `<indent=9%>` hanging indent so wrapped lines align under the text; badge reads "Question n / 5" during a question. ④ `_Recovery` autosave deleted again + gitignored (Unity re-creates it after a crash; the copy owner committed in `99c1909` was pre-`093c33a` and stale) along with the Xbox GDK auto-gen Resources asset. **Owner verified the loop live.** | ✅ done |
| **⚠️ Correction recorded (2026-07-21):** an earlier pass this day argued the Reader's questions were "contaminating" the race by being SWBST-shaped and should be rewritten comprehension-shaped. **That was wrong** — checked the researcher's `Documentation/STORIES FOR SESSION 1-10 ….docx` and their own processing questions are SWBST-shaped by design ("Who is the main character?" = Somebody, …), page-by-page. Reader **pre-teaches** the five slots with text visible; Race asks the same five with text gone. Don't re-derive the wrong conclusion — see the `s01_easy.json` known flag. | ℹ️ |
| **F34 endless race: chaser, scene cleanup, controls** — ⚠️ **①'s chase model is superseded** (F39 replaced the danger-driven band with appear-on-bump; F47ⓑ replaced transform placement with live renderer bounds). What survives from ① and is still load-bearing: the patrol is **unparented** and recomputed from the runner each frame (their track floats its origin ~every 100m), and the visible band is **derived from the live camera**, never hard-coded. ① **patrol wired to danger**: danger was bookkeeping only (nothing read it), so a wrong pick was a silent 1.5s slow. Now it climbs at `mission.dangerPerSecond`, −15 correct / +10 wrong, and drives `PatrolCop` (grey-box capsule fallback, collider stripped). Wrong pick = 1.6s **menace beat** at 85% danger so the chaser visibly closes in. **Never catches** — `timesCaught` stays 0 (D7). Two things *measured*: the patrol is **unparented** (track floats its origin ~every 100m — a world-space follower gets stranded; position recomputed from the runner's transform each frame), and the visible band is **derived from the live camera**, not hard-coded — runner sits at **z=+3**, camera at **z=−3** (so 6m back, not the 3m F27 implies) and 4m up @15°, cone bottoming 44.4° below horizontal ⇒ anything closer than ~4.1m in front of the camera falls out of frame *below* it. Result: off-screen ≤65% danger, edging in at 75%, full at max. Copying the old race's 1.8–12.8m would have parked it *in front of* the runner. ② **loadout stripped**: owner screenshot showed their char/theme/accessory pickers + RUN! + tutorial overlay — the saved scene had all 13 Loadout children **on** (`MaskLoadoutFlash` only hides at runtime ⇒ frame zero + Game view showed it); 10 active ones switched off in-scene. Plus a gate I'd introduced: briefing scrim goes up frame 1 but `HideTheirChrome()` runs ~1.25s later, so a fast START tap exposed their chrome → START now non-interactable reading `GameText.RaceBriefingWait` until `MarkBriefingReady()`. ③ **briefing re-themed** off the grey scrim onto our art (`bg_splash` backdrop, `panel_gold` card, warm-brown type, SWBST chips in-card, Ms. Lumi presenting, card pop-in). ④ **controls**: Android swipe already worked (their touch path); PC was arrows-only → `EndlessKeyboardInput` adds **WASD** via New Input System driving their public `ChangeLane/Jump/Slide` (their script untouched; arrows deliberately left to them — double-binding skips two lanes). Note `activeInputHandler` = **2 (Both)** on this branch vs **1 (New only)** on main. | ✅ done (verify in playtest) |
| **F35 pre-race + kid runner + no crash obstacles** — ⓐ briefing upgraded: gold kit-pill title banner overlapping the mission card, glossy kit-green START + dark ring (`goldPillSprite`/`greenPillSprite` serialized on the director, wired in-scene), Ms. Lumi pop-in + "Ready, runner?" bubble (`GameText.RaceBriefingLumi`), SWBST chips stagger in; **giant gold 3-2-1-GO!** center screen (verified via live window capture). ⓑ **pause owned**: their `GameState` focus/app-pause handlers guarded → menu-less `Pause(false)` in our mode (no PAUSED menu / Quit-to-Loadout over the race); `EndlessRaceDirector.OnApplicationFocus` resumes (timeScale/audio, StartMove only if `_runReleased`). ⓒ **our Mixamo kid replaces Trash Cat**: `KidCharacterAnimation.controller` = their `CharacterAnimation` graph cloned 1:1 (params/states/transitions their code drives; their runLoop tree had a null child → rebuilt as real 3-point `LaneSwitch` lean L/F/R), clips = Idle/Running/RunningJump/RunToRolling/JoggingStumble/FlyingBackDeath; `Bundles/Characters/Cat/character.prefab` = cat meshes+accessories stripped, Ch46 kid in, `Character.animator` → kid's humanoid Animator (name stays "Trash Cat" for save compat; loadout hidden anyway). ⓓ **`TrackManager.SpawnObstacle` guarded out** in our mode (3rd guarded line): GDD says the only obstacle is a wrong answer — no barriers/bins to crash into (root cause of the buggy hit anim owner saw); kid Hit state kept as safety net @1.6× speed. | ✅ done (verify in playtest) |
| **F36 spec-grounded race visuals** — only what TDD §11.4/11.5 actually call for, nothing invented: Hovl Star-hit **sparkle** at the collected card (gold-retinted, auto-cleaned) + amber screen-edge **danger vignette** (procedural radial sprite) ~~whose alpha reads the same danger the patrol does~~ — **superseded by F39**: the vignette rides the menace *surge*, and the `_danger` scalar it named was deleted in F43ⓒ (3 writes, 0 reads). Deliberately *not* added: hop, finish dance, danger meter bar, live score | ✅ done |
| **F38 Aj replaces Ch46 as the race runner** — `character.prefab`'s KidModel meshes + skeleton swapped to the Boy01/Aj Mixamo character, Animator avatar → AjAvatar, `KidCharacterAnimation` unchanged (both are Mixamo humanoids on identical `mixamorig` bones ⇒ every clip retargets free). The old "`Aj.fbx` renders invisible" flag was **stale** — verified valid Humanoid avatar, ~2m bounds, enabled renderers, URP/Lit with `Boy01_diffuse` | ✅ done |
| **F38b race feel fixes** — ① the runner ran in place during 3-2-1 (`StopMove()` halts the track, never the animator) ② the cop floated and never animated: `Running` was set while its GameObject was still **inactive**, and Unity resets animator params on enable → SetBool moved post-activation; the float came from tracking the player's live `y` (which rises with jumps), so the cop now captures flat ground height once on activation ③ gates too close to read → `GameRules.RaceSecondsPerGate` (12s) derives the gap from top speed; a story's `checkpointSpacing` stays a hard floor | ✅ done |
| **F39 patrol chase redesign + disarm cop** — the rifle/holster/doughnut were **static root children** (not skinned or bone-parented), so they floated ~1.4m behind the animated body — that was the "split body / floating weapon" bug, and armed is the wrong tone for a friendly chaser (D7) → `HideCopAccessories()`. Chase rebuilt Subway-style, "appear only on a bump": the cop is a **gap behind the *live* player recomputed each frame** (so a floating-origin recenter is absorbed, no stall or stranding), hidden behind the camera during a clean run, rushing in on-screen while `_menaceTimer` runs, then receding — no longer driven by the baseline danger creep. Vignette rides the surge, not the meter. Retired `PatrolCloseInFront/PatrolFarBehindCamera/PatrolFollowZ/PatrolMenaceDanger` | ✅ done |
| **F40 SWBST inventory tracker** — persistent 5-slot tracker across the top (`BuildTracker`/`RefreshTracker`): beveled wooden plaques on a wood board, empty slots show a faded "?", the current target pulses in its SWBST colour, collected slots fill with the word in order — it teaches the framework *and* shows what's next. A correct pick's word lifts off the card and **flies into its slot** (`FlyCollectedToSlot`) + punch. Banner is now finish-only (the tracker owns progress); the redundant in-world SWBST type pill dropped from both gate builders. The transient **blue dome** chased since F39 = Trash Dash's `TrackSegment.OnDrawGizmos` — Scene view only, never in Game or a build | ✅ done |
| **F40c revert (owner steer) + F40e countdown idle** — owner: *the only pacing change asked for was **track length**, never speed*. Reverted the over-reach — RunSpeed anim param, run-stride sync, sustained boost timer, celebration hop, countdown idle-forcing — back to original Trash Dash feel. Kept the gate runway, tracker, collect-to-slot, cop disarm, appear-on-bump. Idle then done right in F40e: their `WaitToStart` flips idle→run **once on its own ~frame-113 timer**, and their idle state ("Start") is an **orphan with no transitions**, so idle only holds if explicitly re-asserted — now from `LateUpdate` alone (final word before render) with `anim.Update(0f)` on the correction frame, which removed the intermittent "run-back" blip (intermittent because a late START tap hid the flip behind the opaque briefing) | ✅ done |
| **F42 cinematic start camera** — the 3-2-1 now plays on a low tracking dolly that arcs from behind-left up into the exact chase pose on GO!. Two owner-caught iterations first: an orbit to the front (F42b) and a pure side shot (F42c) both framed the **skybox** — the world is only built *ahead* of the kid, so any angle facing sideways, back or up shows blue void. The rule: **pre-race camera stays behind the kid and looks forward down the corridor.** Dance dropped for idle (F42d — "idle is enough"). A `Time.timeScale=0` stall while MCP-driving the countdown is their focus-pause with Unity unfocused, not a code bug | ✅ done |
| **G1 all 30 story JSONs + `Tools/StoryPipeline/`** — the content was never missing: the researcher's docx already holds 10 days × 3 difficulties, 29 with the full treatment, so this was **conversion, not authoring**. 27 new JSONs; `s01_*` preserved as the hand-checked gold standard. The pipeline is versioned because the 30 JSONs are **generated artifacts** — if the researcher revises content, **re-run it, don't hand-edit 30 files** (`extract/parse/emit/apply/flag.py` + `overrides.json`). Distractors needed a real editorial pass, not mechanical derivation: reusing a question's wrong options fails three ways, each a validity threat — register tell, length tell (a 17-word correct answer beside 3-word ones, also unreadable on a moving card) and slot mismatch (a page question isn't always shaped like its SWBST slot). 66 element sets edited; `flag.py` reports **0 of 135** flagged. Every story carries `world` + hero/narration paths. Fixed 2 doc typos learners would have read | ✅ done |
| **P2a StorySelect data-driven** — the controller hardcoded `"s01_easy"` in three places, so 27 of the 30 JSONs could not be opened at all. Cards now build from the session's stories: new `Data/StoryIds` owns the `sNN_<difficulty>` convention in one place (so StorySelect and SessionMap cannot disagree), `GameManager.SelectedSession` clamped to `1..GameRules.SessionCount`, one `DifficultyCard` per card with every reference optional + null-checked (a partly dressed card degrades instead of throwing). Easy always open, each later difficulty waits on the one before; a story that fails to load never presents itself as playable; a locked card nudges, never dead-ends. F22 visuals untouched — plumbing, not a redesign | ✅ done |
| **P2b SessionMap built** — was 0 GameObjects with nothing routing to it. Built code-first via MCP: sky + clouds, gold banner, kit board, **ten stops snaking bottom-to-top** so the path reads as one route, back button, hint. MainMenu's TAP TO START now enters the map instead of jumping to StorySelect. `SessionMapController` mirrors `StorySelectController`'s shape. Locked is a friendly "not yet" — the hint reads "Your teacher opens the next mission!" because sessions open via the teacher's PIN (GDD §8.3), so a locked stop is **never the learner's fault**. Stops are positioned **by fraction of the board**, not absolute pixels for a 1080×1920 reference (which overflowed) | ✅ done |
| **P3a progress persists + the study finally records data** — `GameManager.CurrentLearner` was declared and read but **never assigned** (⇒ `GetBestStars` always 0, `CompleteStory` silently skipped persistence), and `SaveManager.LoadProfiles/SaveProfiles/AppendLog` were fully written with **zero callers** — the plumbing existed, nothing was connected. New `Core/SessionLogService` in `[Core]` builds one `SessionLog` per play-through purely from EventBus traffic (features stay decoupled: they raise, it listens — TDD §8.2), records **only the first answer per page** (a later answer is practice, not a measure), and writes an abandoned run too, recognisable by an empty `finishedIso`, so a dying device costs at most one screen of data. `Bootstrapper` activates a learner before any scene can read progress. `timesCaught` stays 0 **by design** (D7) — not a bug, don't "fix" it | ✅ done |
| **P3b teacher gate (core + scenes)** — `Core/TeacherGate`: HasPin/SetPin/VerifyPin/UnlockNextSession; the raw PIN is **never stored**, only a salted SHA-256 hash in `AppSettings.teacherPinHash`. `SaveManager.ExportLogs()` combines every learner's `.jsonl` into one timestamped file at the root of `persistentDataPath` (returns null when there's nothing rather than writing an empty file); `DeleteAllData()` for the post-study wipe. Scenes built code-first: **NameEntry** (name field + 4 avatars — heart/star/gem/lightning, different shape *and* colour after two near-identical gold stars failed a portrait render; an empty name box is never an error) and **TeacherMenu** (PIN → unlock next session / export logs **with the full path on screen**, since the researcher must find that file over USB / two-tap wipe). Boot routes to NameEntry once on a fresh device (`LearnerProfile.named`), then MainMenu forever; MainMenu carries a discreet low-contrast teacher corner — discreet rather than hidden, since everything behind it is PIN-gated. Session gating is an **internal-validity control** (GDD §8.3, learners must not self-advance), not a convenience | ✅ done |
| **P5 narration for all 30 stories** — 150 clips (30 × 5 pages) generated `en-PH-RosaNeural --rate=-10%` into a staging folder **outside `Assets/`**, then imported in one batch as mono Vorbis **CompressedInMemory** (not decompressed), load-in-background, for the 2GB floor: 6.6MB against a 300MB budget. **No code and no JSON changed** — each filename comes from that page's own `narration` field, so the paths the Reader already asked for simply resolve; before this the game was correct-but-silent (`AudioManager` treats a missing clip as a silent page). All 150 verified to resolve through `Resources.Load` exactly as `ReaderController` requests them. `/NarrationSamples` deleted — the voice is decided | ✅ done |
| **P4 ten race worlds** — `Features/Race/RaceWorlds.cs` = a table of ten recipes (sun colour/intensity/angle, flat ambient, linear fog, sky colour) applied by `EndlessRaceDirector` from the story's `world` field. **A world is data, not a scene** — ~~props keep coming from the theme the track already spawns~~ **superseded by F54**: light-and-fog-only meant all thirty races ran the identical Industrial alley; a row now also selects theme, zone family, sky dome and greenery. Ordered as a journey: `morning_suburbs → bright_park → sunset_town → blue_hour_suburbs → overcast_industrial → golden_fields → night_city → misty_morning → autumn_lane → starlit_finale`, so session 10 feels like arriving. Within a session the three stories share a world but **shift time of day** (easy earlier/warmer, hard later/cooler — light and fog only), so all 30 races differ while a day still feels like one place. Verified by applying all 30 and hashing the resulting fog/ambient/sun/sky: 30 applied, **30 distinct**, 0 duplicates. Fog kept in a sane band near the scene's 50/80 so nothing regresses toward the bleaching F29 had to fix | ✅ done |
| **P0 decontaminate** — banned packages gone from `manifest.json` (ads/analytics/purchasing/GDK; purchasing was still auto-initialising at runtime even with its scripts compiled out); their `Start`/`Main`/`Shop` dropped from Build Settings; **Boot restored to build index 0** (it was their scene, so an APK would have launched into Trash Dash with no `[Core]` singletons); both `OpenURL` objects deleted. Their GameOver/Leaderboard/Highscore/StoreButton/Loadout objects are **still in `MainSummaRace`, inactive** — hidden, not removed | ✅ done |
| **P7/P8/P9** — race-card wording shortened through `overrides.json` (longest element string 74 → **53 chars** (re-measured 2026-08-21: **52 chars**, `s01_easy` SO), matching the s01 playtest baseline exactly; `flag.py` gained `MAX_CARD_CHARS`) · placeholder hero art generated for the 27 stories that had none (**30/30 now resolve**) · session-complete celebration + Results returns to the map | ✅ done |
| **Audit pass (8 findings, all closed)** — ① race lane pattern was fixed at 1,0,2,1,0 every story/run — a learner could score without reading (regression; the legacy race shuffled) ② Reader answer-position bias, B correct 96/150 — now a seeded per-page shuffle, so the researcher's content is untouched ③ difficulty was inert on the shipping race — now drives gate spacing ④ `s09_hard` "Baba Yag a" typo ⑤ build index 0 / `OpenURL` (closed by P0) ⑥⑦ Android identity + min SDK 26 + ARM64 ⑧ `s01_easy` AI-authored questions = researcher sign-off item, **not** a rewrite | ✅ done |
| **F43 shipping-identity + dead-end fixes (this pass)** — ⓐ **app icon**: the build was still shipping **Trash Dash's cat** (`ProjectSettings.m_Icon` → `Assets/UI/StoreIcon.png`); nothing in the game window reveals it, which is why six playtests missed it. Rendered our own from the Boot lockup (crown + SUMMA/RACE! on the sky backdrop) by cloning `SplashCanvas` into an offscreen camera, scaling 0.68 to clear Android's adaptive-icon safe zone, cropping 1024² on the measured logo bounds; set for all 6 densities. `AndroidBundleVersionCode` 350 (their release counter) → 1. ⓑ **a missed FINISH gate trapped the learner in an endless run** — pass-by recovery was guarded `_activeElement < 5` and FINISH *is* element 5, so it was the one gate with no safety net; once missed it died with its segment and `TryPlacePending` early-returned forever, with no pause/exit to escape (see below). Trigger 0.6m → 3m (0.6m = one physics step at maxSpeed 30), FINISH now re-presents like any other gate, plus a 2s stranded-run watchdog. ⓒ dead `_danger` bookkeeping removed — 3 writes, 0 reads since F39 replaced the meter with the menace surge, and its comment claimed the patrol read from it, which had already misled the audit ⓓ `Settings` scene cut (empty, unrouted) + `PlaceholderScreen.cs` deleted + legacy `BuildWorldCard` given the quad fallback | ✅ done |
| **F44 validity + off-happy-path pass (agent fan-out)** — ⓐ **THE RACE COULD BE WON WITHOUT READING.** `correct` was the strictly widest of the three cards in **121/150** sets (mean +10.8 chars); with lane position already randomised, width was the last cue, and "always tap the widest card" scored **84.7%** of gates against 33% for guessing — perfect in 7 stories. Since `raceFirstPickCorrect` *is* the star count and the logged measure, the instrument could not separate comprehension from card-width perception. `flag.py` had reported "0 flagged" because its length rule counted **words** with a ±5 tolerance (fires on 1/150) **and exempted s01 as `GOLD`** — and s01 turned out to be the worst offender of all (margins to +30), i.e. the one session every learner plays first was the one never measured. Rewrote **120 element sets** (distractors only; 7 `correct` lines fixed for being factually wrong about their own story — `s09_hard` said Marusia *found* the flower when she *told Baba Yaga where it was*; three "BUT" slots held no obstacle, so a learner who correctly understands "But = the problem" would have **eliminated** the right answer). Now **45.3%** (re-measured 2026-08-21: **36.0%**, flag.py corpus audit) **/ +1.4 chars, all margins ≤6, flag.py 0 of 150**, gate re-based on characters with `GOLD` empty. Also: distractors no longer reuse the Reader's own wrong options verbatim (was ~50% — the race was passable from memory of the Reader). ⓑ **the project could not build a player at all** — unguarded `using UnityEditor;` in `TrackManager.cs` (harder blocker than the missing Android module) ⓒ **backgrounding discarded the rest of the run's data** — `OnApplicationPause`→`Flush()` nulled `_log` while every handler early-returns on null, so one notification dropped every later page/pick/summary and wrote no completed row; now snapshots with `runId`+`isPartial` ⓓ **a frozen `timeScale` could escape the race** and leave the whole app silent and motionless until restart ⓔ **my own F-pass music dedupe could hang the race** — their `MusicPlayer` *does* have a singleton guard, so dropping index 0 of an unordered `FindObjectsByType` could destroy the live instance → briefing stuck on "Getting ready…" forever ⓕ answer-card triggers had FINISH's old tunnelling margin, and there a physics miss is **recorded as a wrong answer** (0.5→2.5m) ⓖ `speedRatio` went **NaN** on an early wrong pick (maxSpeed clamped to exactly minSpeed → 0/0), freezing lane control ⓗ GO! could start the world at zero speed ⓘ a dead gate's other cards stayed pickable for the rest of the frame ⓙ **Summary's SWBST card covered Ms. Lumi and the prompt** (anchored 0.94 into a header band starting 0.90; caught by offscreen portrait renders, invisible in the landscape game view) ⓚ teacher **delete stayed armed across other actions** — a later stray tap wiped the study; export now writes a companion learner roster (rows stay pseudonymised) ⓛ **120 Trash Dash objects deleted** from `MainSummaRace` (StoreButton/SettingButton/MissionButton/OpenLeaderboard/LoadoutGrid/SettingPopup-with-DataDeleteConfirmation/Leaderboard). **Their `UICamera/Game` chrome is load-bearing — `GameState.UpdateUI()` dereferences it every frame; do NOT delete it.** GameOver + remaining Loadout children need a code guard first | ✅ done (verify in playtest) |
| **F46 playtest pass — the race was unreadable, unbuildable, and could log answers nobody gave** (commit `f044d39`) — ⓐ **the SWBST tracker printed the whole answer sentence into a 138×96 plaque**: "She wanted a turn on the swing" wants **806px in a 118px box**, so autosize hit its 18pt floor and Overflow spilled it across the neighbouring slots. It shows the element **name** now — which is what a tracker is for; the answer already flies into the slot (F40). Floor 24pt, overflow clipped, no mid-word breaks. ⓑ **every gate gap was ~2.3× too long** — spacing was costed against `maxSpeed` **30** while their track starts at `minSpeed` **10** and accelerates at 0.2 m/s², reaching 30 only at ~100s, *past the end of a 5-gate race*. 12 intended seconds of runway became 20–28 real ones of empty road. Now costed against the speed actually being run; `RaceMinGateGap` 150→**110** (at 10 m/s the 150m floor clamped easy/average/hard to the *same* number for the early gates, so difficulty — the one thing gate time drives — was inert exactly where it is most felt; now **15 / 12 / 11s**), `RaceMaxGateGap` 400→**300**, and a re-presented card gets seconds of runway instead of a fixed 18m (under a second of warning at speed: it appeared and was gone three times, then the element was silently written off). ⓒ **two defects that quietly corrupt the thesis measure** — answer-card triggers were 1.6m deep = a **1.27m catch window against 1.0m of travel per frame** at 30fps/maxSpeed, a **27% margin**, and a tunnelled gate is **recorded as a WRONG ANSWER**, so a rendering hitch became study data (FINISH got 3m in F43 for exactly this reason; the answer gates never did) → **3m**; and F43's stranded-run watchdog re-presents the **correct** card, which would then be collected and logged as a **CORRECT first pick for an answer nobody gave** → the first pick is now closed as a miss before the re-present, same as a run-past gate. ⓓ **the F42 start dolly framed a wall** — tuned before F27's alley corridor existed, it started 3.2m left and aimed 4m *past* the kid, so the cinematic was brickwork with the runner half out of frame. ⓔ `PatrolSurgeGap` 1.8→2.1 **— superseded by F47, do not restore either number.** Tuning the gap could never work: placement used the cop's *transform* while his rendered mass sits up to **0.99m ahead of it**, swinging as the run clip translates a 19-part rigid rig, so every gap was wrong by ~0.8m in the direction that causes the overlap. Screenshots read as fine at gaps where the bounds provably intersected — only logging `Bounds.Intersects` found it. ⓕ **`SceneLoader` cleared `_loading` only after the fade**, so every "never a dead end" rescue raised from a scene's `Start()` (Reader/Arrange/Summary/Results bailing to StorySelect on a missing story) hit the early-return and was **silently dropped** — the net added in `f4c3607` was inert. ⓖ Results hid its only exit and re-enabled it at the tail of a coroutine with unguarded indexing inside — one throw stranded the learner for good. ⓗ an empty log could flush as a junk "abandoned run" row, and an editor-run row with no learner id merged into the researcher's export. ⓘ menu music played under the Reader narration for the whole story — narration is the accessibility support the study depends on. ⓙ the VOICE toggle was never `PlayerPrefs.Save()`d. ⓚ readability/layout: lane lean could reach only **43%** of the blend range and peaked after the kid had arrived (LaneSwitch tree never left centre) · Ms. Lumi occupies the middle **44%** of a 2048×1024 canvas so `preserveAspect` rendered her thumbnail-sized with her bubble detached · the tracker sat **26px** into a 1920 reference, under the status bar · feedback text had no backing pill (amber on a sunlit street) · their 5-4-3-2-1 could show alongside ours · Arrange's instruction line was drawn inside the speech bubble's band. ⓛ **build + decontamination**: two unguarded `using UnityEditor;` fixed (see Build blockers below) and the five leftover Trash Dash buttons neutralised | ✅ done (verify in playtest) |
| **F47 the race could not be read, and the chaser was measured instead of eyeballed** (commits `dca9b27`, `04c6cf4`) — ⓐ **THE RACE ASKED FOR ~19 WORDS IN HALF A SECOND.** Two independent derivations agreed: an answer card autosizes to **~1.95pt** ⇒ a **0.1365m** cap height, which clears the 20-arcmin legibility floor on a 10.1" tablet at 40cm only within **11.3m of the lens = 5.8m ahead of the runner** ⇒ **0.50s of legible time at gate 1, 0.28s at gate 5**, against ~**102 characters per gate** (~11.6s of decoding at 100wpm, 16.5s at the 70wpm a struggling reader manages). That is a **validity threat, not polish**: every lane always carries a card so each gate is a forced choice, a run-past logs as first-pick-incorrect, and `raceFirstPickCorrect` **is** the star count and the headline measure — so strong and weak summarisers both converge on a 1-in-3 guess and the instrument stops discriminating, which is the entire point of the control comparison. Note F44 removed the card-width tell that let learners score *without* reading; that tell was the only thing making the race scoreable, so removing it exposed that the cards were **never legible**. **Fix: the cards stop being the reading surface.** `BuildOptionPreview` puts the three options on screen as normal UI text in the sky band (**y 0.745–0.885** — horizon is at 0.7375, the tracker's underside at 0.8958, so it can never cover road, runner, lane halo or tracker), three equal columns in lane order, same plaque/colour/type, filled from the **same shuffled array** the world cards get, so nothing marks the correct one and panel and road cannot disagree. Gate 1 80m→**`RaceFirstGateDistance` 130m** (a track-**length** change — what F40c sanctioned; speed untouched). **Reading time 0.28–0.50s → 9.0–13.7s per gate.** Average meets the 100wpm budget; it does **not** meet 16.5s@70wpm — that needs `RaceSecondsPerGate` 12→17 and is an **owner call**, deliberately not taken. ⓑ **the patrol overlap was a measurement bug, not a tuning one** (see F46ⓔ) — placement now derives from **live renderer bounds** with the gap floor recomputed each frame as `copHalfZ + kidHalfZ + margin`, and he takes the **shoulder** the kid is not on rather than the kid's screen column. Verified in play mode: `Bounds.Intersects(cop, kid)` = **False**, 2.28m lateral / 0.20m along-run separation. **Honest limit:** he is still clipped at the frame's bottom/outer edge — full containment needs a camera pull-back, which stays off-limits. ⓒ **tap-to-move** (`EndlessTouchInput`): tap a third of the screen to go to that lane — **one tap to any lane**, where a swipe was one lane per swipe (the far lane needed two inside a sub-second window) and a hurried diagonal flick was routed to **Jump**. Cannot fight their swipe path: a tap needs <2% drag, their swipe fires at 1%, so no gesture can claim both. `RaceBriefingBody` now names tap **first**. ⓓ **the race finally has a way out** (closes CLAUDE.md open item ②): pause chip in the top-right gutter, two-tap leave, `timeScale`/`AudioListener.pause` restored on **every** exit path incl. focus loss and `OnDestroy`; resume uses `StartMove(false)` so a pause never reseeds `minSpeed` and is never a penalty. Leaving routes through the **existing** partial-run mechanism and flushes immediately, because the next `StoryStarted` may be a different child. ⓔ **which distractor was chosen is now logged** (`racePicks`, schema 2→**3**, purely additive) — the last irrecoverable data gap; without it the researcher could see *that* a learner was wrong at "But" but never *which* misconception they held. Also `raceFirstOutcome` was being filled by **re-present** collections, so a gate the learner never chose read as `"correct"` on every abandoned run. ⓕ **"SOMEBODY" was ellipsised in the tracker** — introduced by F46ⓐ itself (24pt floor + Ellipsis; that word needs **128.3px in a 118px** plaque), first slot of all 30 races. Plaques 138→**160**, gap 12→10, board 774→880; floor stays 24pt. ⓖ **the F44 validity crisis was still live in the READER**, which F44 never touched and from which the race distractors were derived: correct was strictly the longest option in **124/150** questions (mean **+10.5** chars), and "always tap the longest" scored **81.3%** vs 33.3% guessing. Reader first answers are study data (`readingFirstCorrect`) and the shuffle randomises the three *buttons*, never the three *lengths*. **112 distractors extended** (never replaced, never touching a correct answer) ⇒ rank 50/53/47, mean −2.4, strategy **32.0%** = chance. ⓗ **learner language**: "Locked in" (Arrange) meant *correct* while "Locked" everywhere else means *you can't have this*; "Your Mission" titled the race while mission = **session** on four other screens; Results said NEXT MISSION mid-session while routing back to the same session's cards; 9 idioms an ESL reader has no reason to know; praise had drifted from **process** to **ability**. ⓘ **first automated tests in the project's history** — `Assets/_Game/Tests/`, **45/45 green in ~1.8s**. No asmdef: one **cannot** reference `Assembly-CSharp` (predefined assemblies auto-reference every asmdef, so the reverse is a cycle) — the fixtures live in `Assembly-CSharp-Editor`, which already gets NUnit via Auto Reference. The load-bearing fixture guards the widest-card tell in the build instead of in a script someone must remember to run | ✅ done (owner playtest pending) |
| **F48 atomic saves** (commit `e5966be`) — `SaveManager` used a plain `File.WriteAllText`, which **truncates the real file and then streams into it**. A tablet that dies, is force-quit or is yanked off charge inside that window leaves `profiles.json` truncated; the read path catches the parse failure and returns an **empty list**, so the learner silently boots as a brand-new child — name, avatar, every star and every unlock gone, with nothing on screen to say so. Across 40 tablets × 10 sessions that is not hypothetical, and progress is what tells the teacher which session a learner is on. Now: temp file → single `File.Replace`, previous good copy kept as `.bak`, reads fall back to `.bak`; the old direct write stays as a last-resort fallback so this can never be worse than what it replaced. `DeleteAllData` had to follow — the `.bak`/`.tmp` siblings hold the same learner data, so the post-study wipe (which **is** the consent promise) would have left named children on a tablet believed clean. `settings.json` is **deliberately kept** by that wipe because it holds the PIN hash, and a wipe must never hand a tablet back un-PINned; its stale `.bak`/`.tmp` do go, so a backup cannot resurrect a superseded PIN | ✅ done |
| **F49 colour was the only channel** (commit `06f1778`) — two strings named a colour as the sole carrier of meaning: Reader after a wrong pick (*"the green one is the answer!"*) and Arrange when stuck (*"The green ones are right"*). A red–green colour-blind learner is told nothing by either — ~8% of boys, so 1–2 children in a 40-learner study, on exactly the two moments that exist to help someone already struggling. Now `GameText.ReaderWrongFeedback` = *"Not quite — here is the answer!"* (the correct option is already highlighted **and** punch-scaled, so motion carries it independently of colour; deliberately **not** a tick glyph — there is no tick in `ReaderController`'s UI to point at, and naming one would be a worse lie than naming the colour) and `GameText.ArrangeAlmost` = *"the parts already in place are right"* (a correct slot is physically locked and stops responding to taps — perceivable by everyone). The rest of the SWBST palette always carries a letter or word alongside the colour; these two lines were the only real failures | ✅ done |
| **F50 nine silent interactions + every rejection sounded like success** (commit `38fe7ee`) — `TeacherMenuController.Submit()` played its click **before** it knew the answer, so a wrong PIN, a PIN mismatch, the 30s lockout and — worst — *"there was nothing to export"* all answered with the same cheerful click: the researcher could tap Export, hear success and walk away with no file, on the one action that retrieves the entire dataset. 11 sites now route through `Reject()` (status line + warm nudge, never a buzzer); success got its own distinct sounds. The important class of fix is feedback that was **audio-only**, because a classroom tablet is usually muted and a tap that answers with nothing reads as a broken button: Reader's HEAR AGAIN was pure audio, Arrange's empty-slot tap and no-op UNDO clicked and did nothing, Summary's DONE TYPING was silent, and NameEntry — the first screen on a fresh device — had no music. Punches go on the **label**, never the root (the root carries `ButtonSquash`, and two tweens driving one `localScale` leave it wherever the last one wrote). Also `AppSettings.narrationVolume` was declared, persisted and read by **nobody** (`PlayNarration` hard-coded `1f`) — narration is the accessibility support the study depends on. Evidence pack: `Documentation/SummaRace_Audio_Audit.md` | ✅ done |
| **F51 BACK quit the app, and NEXT could zero a study field** (commit `46bbb90`) — ⓐ Android **BACK closed the app mid-story** with nothing handling it (zero `escapeKey`/`Escape` handlers project-wide, `androidPredictiveBackSupport: 1`, so Unity's default finishes the activity). Two things make it a daily event across 40 tablets: on gesture navigation BACK is an **edge swipe — the same motion the race trains** — and on three-button navigation it is a permanent target under a portrait game where a child's thumb already is. A learner dropped to the launcher mid-story has their run filed as abandoned. `Core/BackButtonGuard.cs` on `[Core]` (added by `Bootstrapper`) reads the key each frame and does nothing with it, which marks it handled. **Deliberately not wired as an in-game "back"** — every leavable screen already has its own control with the right rules (the Reader's exit is offered only before the first answer, because after that the run is study data), and a hardware button on those would reopen the dead-end and data-loss paths those rules close. ⓑ a **double tap on Reader NEXT silently zeroed `readingSeconds`**: `Advance()` left NEXT visible and interactable on the last page, so two thumbs dispatch it twice; `SceneLoader`'s guard blocks the second scene *load* but not the second *event*, and `SessionLogService` had already run `ClosePhase()`, so the second `ReadingCompleted` overwrote the phase clock with ~0 — the row exports as perfectly valid and the variable just reads zero. Now latched and the button hidden. ⓒ the Reader's **correct answer rendered greyed and half-transparent** on the single most important teaching beat: `interactable` was set false *before* the colour, and those buttons use ColorTint whose disabled colour (0.784 grey, α 0.502) multiplies the image. Re-entry was already blocked by `_questionAnswered`, so `interactable` was only ever tinting | ✅ done |
| **F52 participant codes — the join to the paper test** (commit `9d839ba`, log schema 3→**4**) — the outcome measure is a **paper pretest/posttest**; the app's logs are the process data, and the results chapter depends on joining the two. That join rested on `displayName`, **a name typed by a nine-year-old** (`learnerId` is a tablet-minted guid that appears nowhere on paper). A child who spells their name differently from the booklet, abbreviates it, or shares "Maria R." with a classmate produces rows that cannot be matched to their own pretest score — and nothing detects it at the time; it surfaces during analysis, when the study is over. Learners now carry a **participant code** (the researcher's own id from the child's booklet), `LearnerProfile.participantCode` + `Data/ParticipantCodes` (normalise/validate, 2–12 alphanumerics), set by the **teacher behind the PIN**, never by the learner. Captured where the study cannot proceed without it: passing the PIN diverts to the prompt when the active learner has none, **"+ New learner" asks for the code *before* Name Entry**, duplicates are refused while the teacher still holds both booklets (`GameManager.SetParticipantCode` → `Saved/NoLearner/Invalid/Duplicate`), and Export warns if any learner lacks one or two share one. The child's experience is byte-identical. Schema 4 stamps the code on **every log row**, not just the companion roster — the roster is a separate file the teacher must remember to pull off the tablet, and if only the `.jsonl` is retrieved the join is severed again. Rejected alternative (recorded so it is not re-proposed): teacher types the code into Name Entry — `Bootstrapper` routes there exactly once, so spending that visit on a code means the child never types a name or picks a runner, `P7` on one tablet and `P07` on another recreates the same failure one step earlier, and `displayName` would then mean "a child's name" on some tablets and "an id" on others with no way for the analyst to tell which | ✅ done |
| **F53 page↔slot alignment audit** (commit `4a78905`) — the support-removal ladder means the Reader pre-teaches a SWBST slot with the text visible and the Race asks **that same slot** with the text gone; the per-slot contrast **is** the analysis. But nothing enforces the alignment — `pages[]` and `elements[]` are two independent arrays and only same-index ordering makes page 3 "the But page", and nobody had ever checked it held. All 150 pairs audited: **135 ALIGNED (90%), 8 WEAK, 7 MISALIGNED**, so the contrast can be reported — which was genuinely in doubt. "Wanted" is the weak point (4 of the 7, all on page 2, where the passage has moved to the first event while the slot still points at the protagonist's goal). **Two of the seven were regressions we introduced**: the P7 card-shortening pass changed what two stories mean — `s04_easy` BUT became a different claim about the story, and `s10_easy` SO dropped "earthquakes", the single thing page 4 is about, importing "tides" from page 5 instead. Both restored to the researcher's wording (SO re-trimmed to 49 chars so it still fits the 53-char card budget). 145 of 150 questions are verbatim from the docx, so the remaining misalignments are in the **source document**, not pipeline drift; six page questions were rewritten toward their slot and are flagged sign-off items. **No SWBST `correct` line was ever bent to match a question** — the elements are the researcher's framework and the summary target. `s03_hard` p2 deliberately left broken (its slot is the Giant's want and page 2 never states it; inventing a question would be writing the researcher's content for them). `apply.py` could previously repair a question's *options* but not the question *text*, which is why this class of defect had no fix path. Verified: 5 files, 33 lines, 0 unintended changes; `flag.py` 0/150; widest-card heuristic unmoved at 50.8%; Reader "tap the longest" 32.0%→31.0% (re-measured 2026-08-21: **29.3%**). Evidence: `Documentation/SummaRace_Story_Alignment_Audit.md` | ✅ done |
| **F54 all thirty races opened on the same alley** (commit `63e1be3`; `SummaRace_Pro_Review.md` calls this the "F48 world-dressing pass" — same work) — `TrackManager` picks zone 0 and rotates only every **500m**, but a 5-gate race is ~840m and its **first Update spawns ten segments at once**, so every one of the thirty races opened on the identical Industrial corridor; P4's worlds only ever changed sun, fog and sky. Meanwhile a **complete second theme — `NightTime`, with its own Industrial/Suburbs/Urban/Sky/Obstacles — had been in the project, fully built, never once instantiated.** A world now selects **theme + zone family + sky dome + greenery density** as well as light (see **Race world & road** below for the table). Each mechanism was a trap: the zone is forced from **`[DefaultExecutionOrder(-2000)]`** on `Features/Race/Endless/EndlessWorldDressing.cs`, *not* a coroutine (a coroutine resumes **after** Update, and the zone has to be right **before** their first one), then re-asserted every `GameRules.RaceZoneHoldSeconds` (5s) by stepping `ChangeZone()` a full lap, which also resets their 500m counter; new **`SummaRace/SkyTint`** shader **lerps** the vertex-coloured dome toward the world's sky colour, because a multiply can only *darken* blue and made overcast/misty/golden skies literally unreachable; greenery uses the **theme's own** `Tree01`/`GrassClump`, already vertex-coloured on Unlit/CurvedUnlit, so it bends with the curved world instead of detaching from the bent horizon (the F27/F29 trap); the colour grade was tuned when every world was daylit so it *manufactured* night by darkening — on genuinely dark night art it stacked two nights and the runner became a silhouette, so exposure now floors at −0.35; and the shader is a **serialized reference, not `Shader.Find`**, so a player build cannot strip it (invisible in the Editor, only appears on the tablet). Measured, not eyeballed: zone holds past two 500m boundaries (ran to 1183m, all ten live segments still Suburbs); replaying the real placement code over 200 segments put **0 of 1792 props inside \|x\| < 3** against an outermost answer card at \|x\| 2.2. RAM unchanged (one theme loads per race; NightTime substitutes rather than adds, both untextured). **Draw calls up to +200 worst case, UNMEASURED on device — `GameRules.RaceMaxSceneryPerSegment` (14) is the first number to halve if the race misses 30fps.** Also fixed here: **races 2 and 3 of every session had no music at all** — `MusicPlayer.RestartAllStems` runs once per app launch and their only other restart is gated on a condition true only the first time, so the director's FINISH silence was never undone (verified live: 6 sources → stopped → 0 → restarted → 6). **Still not honestly a park**: `bright_park`, `golden_fields` and `autumn_lane` are dressed streets, because neither theme contains a park, a field or a country lane. 45/45 EditMode tests green | ✅ done (verify in playtest) |
| **NEXT:** ① **install Android Build Support, then build Addressables content for Android, then build an APK** — both are hard blockers: without the module nothing builds, and with `m_BuildAddressablesWithPlayerBuild: 2` and no `aa/Android` content the APK boots into a race with **no road, no scenery and no runner** (the race loads all of it through Addressables, which resolve off the Asset Database in the Editor so it looks perfect). Run `SummaRace ▸ Build Preflight` first — it checks both, plus the whole scene list, player settings and content integrity, without needing the module installed ② **owner full-loop playtest on a non-s01 story** — 27 of 30 have never been run through the loop even once ③ **prove the USB export path on a real tablet BEFORE day 1** — `persistentDataPath` is `Android/data/<pkg>/files`, which Android 11+ usually hides from MTP; the logs are the entire dataset and there is no second chance to collect them ③b **set a teacher PIN *and* a participant code per learner during install** (F52) — the code is the join key to the paper pretest/posttest and is teacher-set behind the PIN; Export warns about missing/duplicate codes, but a run logged without one cannot be joined afterwards ④ **one tablet per learner vs shared** — open design call; teacher-gated learner switching now EXISTS (picker in TeacherMenu), so both are supported ⑤ **reading time for the slowest readers** — the race now gives 9.0–13.7s per gate, which meets the ~11.6s a 100wpm reader needs but NOT the ~16.5s at 70wpm; `RaceSecondsPerGate` 12→17 buys it, deliberately left as an owner call ⑥ ~~real hero art ×27~~ **DONE 2026-08-19** — all 30 story cards now carry real art ⑦ `Race.unity` (legacy) is still build index 6 with nothing routing to it — **51.5MB** exclusive (not the 23MB previously recorded), 29MB of which is the Ch46 kid Aj replaced. Full remaining-work detail lives in `Documentation/SummaRace_Finalization_Plan.md` and the front door is `Documentation/SummaRace_Owner_Handover.md` — **don't restate them here** | ⬜ next |
| **APK budget (estimated from disk — no APK has ever been built)** — `Assets/` is 945MB but only **238MB is reachable** from the 12 built scenes + `Resources/` + Addressables; the big packs (`Gems and gold` 145MB, `StarterAssets` 84MB, `20 Logos PSD` 35MB) are unreferenced and cost **nothing**. Asset payload ≈143MB shipped + 55–90MB engine/IL2CPP ⇒ **~190–230MB against the 300MB cap**. Free reductions if it ever gets tight: drop dead `Race.unity` from Build Settings — **51.5MB of source assets are exclusive to it** (~~23MB~~, corrected by `SummaRace_Device_Budget.md`; 29MB of that is `Ch46_nonPBR.fbx`, the kid Aj replaced in F38 ⇒ 12–20MB estimated APK saving. The scene *file* is only 119KB — the 51.5MB is its dependency closure, and `BuildPreflight.cs` still prints the old ~23MB estimate); `music_race.ogg` is **byte-identical** to `STEMSMainTrackMono.ogg` (MD5 `ab3867fa…`, 7,277,424 bytes each) and both ship — 6.9MB; `sfx_coin.ogg` and `sfx_collect.ogg` are also byte-identical but only ~25KB each; three textures import **uncompressed** (~9MB incl. `UISpritesheet.png` at 6MB); `TextMesh Pro/Examples & Extras` ships via its own `Resources/` (2.7MB). Deleting unused Addressables groups saves **~0** — their meshes/textures are shared, measured. `.git` is 1.68GB (history of the Endless Runner import/revert) — no APK impact, but slow clones. **RAM, not APK size, is the real risk** — see the Rendering section and `SummaRace_Device_Budget.md` | ℹ️ |
| **F56 the first APK would have trapped every learner, and shipped on an untested renderer** (this pass; audits by fan-out, then fixed) — ⓐ **the race briefing was an inescapable dead end and the first APK was GUARANTEED to hit it**: `EndlessRaceDirector.Start` waited unbounded on `PlayerData`/`TrackManager`, on a screen where START is disabled, the pause chip is not built yet, touch no-ops without a TrackManager and `BackButtonGuard` swallows BACK — and `TrackManager.instance` stays null forever when the Addressables load fails, which is exactly what an unbuilt content build does. Bounded (`BootWaitSeconds`) + a real way back to StorySelect (escapable, **not** playable). ⓑ **`SceneLoader` silently dropped every rescue raised from a scene's `Start()`** — the comment justifying F46ⓕ was wrong about Unity's player loop (integration and `Start()` both run in EarlyUpdate; a `yield return null` coroutine resumes later the same frame), so all four "never a dead end" bails were inert. Requests are queued now. ⓒ MainMenu returned before wiring a single button. ⓓ **the APK was set to render on Vulkan**, which nothing has ever run on — now OpenGLES3-only; `androidPredictiveBackSupport` 1→0 (it fights `BackButtonGuard`); `Link.xml` preserves `System.Security.Cryptography.*` because `SHA256.Create()` reflects and IL2CPP strips it — that call gates the PIN, session unlock and **Export**, and a throw inside a UI callback is swallowed. ⓔ four saves reported success without checking, and **Export reported a FAILED write as "No logs to export yet"**; the export now also backfills `participantCode` onto rows written before it was set. ⓕ **the post-study wipe could put an erased child's row back** (`DeleteAllData` → `InitProfiles`, then the in-flight run writes and recreates the logs folder) → new `AllDataErased` event that discards rather than flushes. ⓖ **on a 4:3 tablet the reading panel covered the bottom 22% of every tracker plaque and 50px of the pause chip** — tracker/chip were pixel-anchored to the screen top while the band is fractional, so they only agreed at 9:16; both anchor to the band now, byte-identical at 9:16. ⓗ pause chip 29dp→**48dp** (tracker slid 52px left; narrowing it was refused — `fit.py` measures SOMEBODY at 140px in a 140px slot, zero headroom); HUD banner was drawn **under** the option-preview board; Name Entry had no way to dismiss the keyboard; the Results title crossed the trophy in **22 of 30** stories. ⓘ `ArrangeController.VerifyRoutine` could strand `_busy`; `EventBus.Raise` now guards its subscribers (a throwing handler killed the *caller's* coroutine); `ParticipantCodes.Normalize` truncated a 14-char code to 12 and accepted it. ⓙ **instructional narration** for ~10 interface strings (Arrange/Summary/loading tips/briefing) — story pages already had all 150. ⓚ `fit.py` now READS tracker geometry from source (it had been reporting a failure the game fixed in F47); `BuildPreflight` gained prefab + package scanning, unqualified-editor-type detection, widened offline patterns and 4 new checks; `OpenURL.cs` deleted (last live `Application.OpenURL` in player code); new EditMode tests for offline compliance, the export backfill token and the narration array | ✅ done (owner playtest pending) |
| **F58 Ms. Lumi unified, a real theme, and a cue that painted the wrong thing** (this pass) — **a** she was **three different women**: Reader drew from the 23-pose pool, the race briefing hard-loaded the old flat-vector `mslumi_wave`, and Arrange + Summary were scene-wired to `teacher_temp.png`, a high-bun woman in a different painted style. First, **the normalizer had never run** — all 23 pose metas sat at import defaults (`alignment: 0`, pivot 0.5/0.5, ppu 100) while `MsLumiReactor.ApplyPose` sizes her rect as `(w/ppu, h/ppu) * headPixels` and anchors on the pivot, so across poses of 162x243 to 345x222 her size swung ~+/-50% and her head jumped every change; ran `SummaRace > Normalize Lumi Poses` and cross-checked every pivot/ppu against an independent re-implementation of the hair-mask algorithm. Then the three legacy images were **rebuilt from the poses** (`missingassets/derive_lumi.py`), no new art. **The rule that matters: the half-bodies are fitted to the original's alpha ENVELOPE, not to a matching head size** — head-matching was tried first and put her silhouette at **49% of the canvas against the 44% the briefing's tuned anchors assume**, which on screen overlapped the START button; the script hardcodes the measured originals rather than reading the file it overwrites, which would drift on every re-run. Canvas 2048x1024 -> **512x256** (and the badge 290 -> 512) because the poses are 162-345px, so a 2048 canvas was a 4x upscale of content that renders at ~547px. **b** **badge pool + the reaction that was dead code since it was written**: `reactToArrangeVerified` / `reactToSummarySubmitted` have always been on `MsLumiReactor`, but **only `Reader.unity` ever carried the component**, so both switches did nothing. 14 disc-cropped badges derived into `Resources/UI/Lumi/` as pools `badge_*` / `badgecheer_*` / `badgemood_*` (pool = filename prefix, so this needed **no** `LumiExpressions` change), plus `MsLumiReactor.AttachBadge()` called from `ArrangeController` / `SummaryController`. `headPixels` stays **0**, which is what makes it safe: `ApplyPose` then swaps the sprite and returns **without touching the RectTransform**, so each scene's own framing survives. **9 of 23 poses deliberately excluded** — `lumi_sad`/`lumi_stern`/`lumi_worried`/`lumi_arms_crossed` by **D7 (never punish)**, which should keep them unused game-wide; 3 for arms wider than tall (crop badly in a circle); 2 caught by a new flat-row guard after their torso-extension row turned out to be hair and smeared into vertical stripes. **c** **`Constants/Theme.cs` — the game had no theme.** Measured across live UI scripts: **84 colour literals holding 58 distinct RGB values**, and the brand gold alone existed as **10 values, 9 of them used exactly once**. Conventions did exist (navy `1C2B54` 5x, brown `593F19` 4x) but had no name, so nothing propagated them. 12 tokens, each set to **the shade already used most often** for that intent; **33 literals migrated, largest shift 0.029 of a channel (~7/255, invisible)**. The other 51 were left alone on purpose — snapping them would have shifted up to 0.070 and some are semantic, not drift. Excluded by design: `SwbstPalette` (a teaching device, must never be re-tinted), `RaceWorlds` (per-world data whose job is to differ), dead `RaceController.cs`. WCAG measured and **written into the file**: 12 of 15 pairs pass AA, and **gold on cream is 1.18 / GoldDeep 1.34 / AmberWarn 1.61 — never as text**, only as fill. **d** **the option panel's arrival cue was painting a wash, not a rim** — `ArrivalGlow` was a **child** of the board, and uGUI draws a child after its parent's graphic, so the gold covered the panel's wood interior with only the later-added columns masking it, twice a gate for every gate of all 30 races. Both comments there promise "border-only" / "a rim BEHIND the board (first sibling)" and the -16px inset only makes sense behind; now a sibling inserted at the board's own index, which preserves every other element's relative order (the HUD banner has already been fixed once for drawing under this board). **e** `GameRules.RacePreviewCueSeconds` deleted — **0 readers**, so tuning it would have silently changed nothing, and it documented the pulse as **1.63Hz** where the coroutine that actually runs it computes **1.85Hz**. Removing a public const needed the **editor** assembly compiled too, since all 10 test fixtures live in `Assembly-CSharp-Editor`. **Verified:** both assemblies compile clean (runtime + `Assembly-CSharp-Editor`), Unity console **0 errors** after reimport, **14/14** badges auto-normalized by the `AssetPostprocessor` hook. **NOT seen running** — the badge reaction, the panel rim and the 9 theme-touched screens all still need a portrait/Play-mode eyeball. | ✅ done (owner playtest pending) |
| **F59 the dark pass overshot, and yes/no was stacked** (commit `b8a8600`) — four corrections from the owner's 2026-08-22 device screenshots, all against work landed the same day. ⓐ **`Theme.Wood` / `WoodLight`**: taking the furniture off saturated kit yellow was right; painting all of it `TextBrownDeep` was not — that token was authored as HEADING TEXT on cream and sits at luminance **0.026**, very nearly black. As a *fill*, against this game's bright art, ten surfaces read as holes punched in the screen and the Session Map looked entirely switched off. Wood is **3.6x lighter** (L 0.026 → 0.096) and still carries cream at **6.6:1**, gold at 5.2:1. **Two jobs, two tokens — do not merge them**: TextBrownDeep is still right for text on cream (11.9:1) and would fail as a fill; Wood is right as a fill and too light to write body text in. ⓑ **yes/no side by side, NO left and YES right** — the fork shape a yes/no actually is, and the ordering Android itself uses, so muscle memory from the rest of the tablet keeps working; applied to **both** confirmations (race leave, `BackButtonGuard`) so the two cannot disagree about which side means what, and the labels now say which answer they are. The blocked form keeps its single button and **re-centres** it, since a lone off-centre button reads as half of a pair that failed to draw. ⓒ **Arrange arms every empty slot** (brighten + 4% lift) while a piece is held — tap-piece-then-tap-slot is only discoverable if the second target announces itself, and *nothing* changed between "nothing selected" and "a piece in my hand". **Every** empty slot, never the correct one: this screen tests sequence, and lighting the right box answers it for them. ⓓ **contrast, measured not eyeballed** — the finish reveal and the briefing chips put `SwbstPalette` **ink on the element's own colour**, and ink is `Lerp(colour, black, 0.45)` = exactly 55% of the background in every channel, so all five rows sat at **2.5–2.9:1** and the answers were invisible on device. Both now use the pairing that variant was measured for (cream plaque, ink text, **5.2–7.4:1**), with the element colour surviving as a leading-edge strip. Also: the patrol entered his run via a *transition*, so the first frames of every wrong-answer beat drew a **standing man sliding down the road at 10–30 m/s** — `Play()` with a random phase enters mid-stride. **Verified**: `Assembly-CSharp` rebuilt after the last edit, Unity console **0 errors**, all four new members present in the loaded assembly. **NOT seen running** — portrait renders need the editor assembly, still blocked by the Adjoint/MCP collision | ✅ done (owner playtest pending) |
| **⚠️ Build blockers** — ① **Android Build Support is not installed** (no APK can be produced at all) and ② **Addressables content has never been built for Android** (no `aa/Android`) — without which **the race never starts**, not merely renders empty. `m_BuildAddressablesWithPlayerBuild` is now **1 (BuildWithPlayer)** so a player build produces content automatically. Detail + how each was verified in **Build blockers** below | ⬜ owner |
| Phase G (30 stories) and I (PIN / logging / export) are **done** above. Remaining: H asset pass — **only the 27 real hero images are genuinely outstanding** (`missingassets/HERO_PROMPTS.md`). Ms. Lumi is **done** (F58: 23 poses + 14 badges, one character on every screen); the 5 race pickup icons are **obsolete**, superseded by the F40 tracker plaques; weather/mood was never built and is out of scope · J device builds | ⬜ later |

NameEntry, SessionMap and TeacherMenu are built and reachable (P2b, P3b). **Settings was cut** — it was never built, nothing routed to it, and the narration toggle learners actually need lives in the Reader (`AppSettings` still carries volumes/haptics in the model). SampleScene deleted (GDD D18 done). Build list is 12 scenes, Boot at index 0.

### ✅ THE BLOCKERS BELOW ARE CLOSED — an APK exists (2026-08-21)

**Everything in the "Build blockers" section that follows is superseded.** The owner's
2026-08-21 playtest was done on an **Android phone**, not in the Editor: the race ran with road,
scenery, runner and theme present, which is only possible if the Addressables content build
resolved, and `Library/Bee/Android/Prj/IL2CPP/Il2CppBackup/` on disk confirms an IL2CPP player
build ran. So Android Build Support is installed and `aa/Android` content has been produced.
The two acceptance numbers (APK ≤ 300MB, 30fps in the race on the 2GB floor device) are still
estimates — an APK existing is not the same as an APK measured.

### ⚠️ BLOCKER, half closed (re-measured live 2026-08-22)

**The URP half is FIXED.** `com.unity.render-pipelines.core@e6c93b445dd3` is now in
`Library/PackageCache` alongside `universal@580a03820d50`, the `CS0246: GenerateHLSL` wall is gone,
`Unity.RenderPipelines.Core.Runtime.dll` is published, and **`Assembly-CSharp.dll` builds** — so our
MonoBehaviours load, MCP resolves them, scenes are safe to save again, and runtime C# can be
verified against the live Editor rather than offline. The old text here said the Editor could not
compile this project at all; that is no longer true and should not be quoted.

**The second collision is still live, and it is what remains blocked.** `com.adjoint.editor`
(added to `manifest.json` on 2026-08-22, uncommitted) ships a precompiled `Adjoint.Editor.dll`
that exports `Helpers.AssetPathUtility`, and so does `MCPForUnity.Editor` from
`com.coplaydev.unity-mcp` — Adjoint is a fork of that same bridge. The Adjoint package has **no
asmdef**, so its loose `Editor/Windows/Adjoint/AdjointToolbarButton.cs` compiles into our
`Assembly-CSharp-Editor` where it can see both, and line 156 fails `CS0433`. Consequence, verified:
**`Assembly-CSharp-Editor.dll` is not in `Library/ScriptAssemblies`**, so the whole EditMode suite,
`SummaRace ▸ Build Preflight`, and the offscreen portrait render (the only way this project has ever
caught a layout bug) are all unavailable.

**Remedy is one line and it is the owner's call, because it is his tooling:** drop
`com.coplaydev.unity-mcp` from `Packages/manifest.json` and restart the Editor. Adjoint bundles the
same bridge, so the duplicate is what created the clash. Do not attempt it mid-session from inside
the agent — if the running bridge is the one removed, Unity access dies with it.

Full evidence for the original diagnosis: `Documentation/SummaRace_Owner_Playtest_2026-08-21.md` §0b.
Offline compilation with Unity's own Roslyn against its own `.rsp` files still works and is the
fallback whenever the Editor is unavailable.

### ⚠️ Build blockers — both real, re-verified 2026-08-06

**Preflight was run 2026-08-20 and scored FAIL 2 · ERROR 0 · WARN 2 · PASS 31.** The two FAILs are exactly the two blockers below (Android module, `aa/Android` content) — everything else the tool checks is green, including all 30 stories through the loader, 150 narration paths, 31 audio keys, portrait lock, OpenGLES3, IL2CPP/ARM64/API 26, predictive-back off, the Link.xml crypto preserve, fog shader stripping, and 0 unguarded `using UnityEditor` across 253 player scripts. The two WARNs: the 18 Android adaptive-icon slots were empty (layers now exist at `Art/UI/app_icon_adaptive_bg.png` + `_fg.png`, 432x432, logo inside the 62% safe zone — they still need assigning in Player Settings ▸ Android ▸ Icon ▸ Adaptive), and Google EDM is still in `Assets/` (editor-only, resolves nothing, self-resurrects — delete with the Editor closed if you want it gone). ⚠️ Operational note the preflight surfaced: the APK will be **debug-signed**, and Unity's debug keystore is per-machine — **build every study APK on the same machine**, or a mid-study update refuses to install over the deployed build and needs an uninstall, which erases learner progress and every unexported log on that tablet.

Run **`SummaRace ▸ Build Preflight`** (`Assets/_Game/Editor/BuildPreflight.cs`, 11 check sections,
read-only, never throws) before every build — it reports both of these plus the scene list, player
settings, app icon, all 30 stories through the real loader, banned packages, offline violations and
`using UnityEditor;` leakage, **and it runs without the Android module installed**.

1. **Android Build Support is not installed.** Unity is `6000.4.1f1` (`ProjectSettings/ProjectVersion.txt`) and `Editor/Data/PlaybackEngines` holds only `windowsstandalonesupport` — there is no `AndroidPlayer`. (The 6000.4.0f1 install on this machine has only `WebGLSupport` + standalone, so it is not a fallback either.) **No APK has ever been produced**, so the two acceptance numbers that need one — APK ≤ 300MB and 30fps in the race on the 2GB floor device — are **estimates, not measurements**. Owner action: add the module (+ OpenJDK + SDK/NDK) via Unity Hub, Editor closed.
2. **Addressables content has never been built — for any platform.** `Library/com.unity.addressables/` contains only `AddressablesBuildTEP.json` (a fast-mode *play-mode* entry, not a content build), there is **no `aa/` folder**, no `Assets/StreamingAssets`, no `ServerData/`. The race scene is Trash Dash's `MainSummaRace` and its **road segments, themes, zones, scenery and the runner prefab all load through `Addressables.InstantiateAsync`**. In the Editor those resolve straight off the Asset Database, so it looks perfect.

   ⚠️ **The consequence is worse than "an empty race", and this correction matters** — the earlier wording here ("no road, no scenery and no runner") described a race the learner could still stand in. Traced properly: `TrackManager.cs:194` `Addressables.InstantiateAsync` returns null → `:197-201 yield break` → the `gameObject.SetActive(true)` at `:228` never runs → `TrackManager.Awake` never runs → **`TrackManager.instance` stays null forever** and the race never starts at all. `EndlessRaceDirector` used to wait on that unbounded, on a screen with no working control (F56 bounded it and added a way back, so the failure is now escapable — it is still not playable).

   **`m_BuildAddressablesWithPlayerBuild` is now `1` (BuildWithPlayer)**, so a player build produces content automatically; it was `2` (DoNotBuildWithPlayer), which made "build an APK" and "build the content it needs" two unlinked actions. The owner still has to run one build with the platform switched to Android. Full detail: `SummaRace_Build_And_Release.md` §3.

   ⚠️ Also on disk: **13 of 15 bundled Addressables groups have empty build/load paths and provider types**, including all six the race needs (`Themes`, `Default-Zones`, `Night-Zones`, `Default-Sky`, `Night-Sky`, `Duplicate Asset Isolation`) — inherited from the Endless Runner import (`e4de43e`). `BundledAssetGroupSchema.OnEnable` very likely re-seeds them to Local*, which is presumably why the sample ever built, so the preflight reports this as a **WARN**. Check it once in the Groups window *before* build day, not on it.

**Standing rule, not a blocker (three instances found and fixed):** a runtime script must never carry
an unguarded `using UnityEditor;`. F44 fixed one in `TrackManager.cs`; F46 fixed two more —
`Assets/Plugins/ithappy/Animals_FREE/Scripts/CreatureMover.cs` (import removed, unused) and
`Assets/TextMesh Pro/Examples & Extras/Scripts/TMP_TextInfoDebugTool.cs` (wrapped in
`#if UNITY_EDITOR`). Either one fails the **player** compile outright while the Editor stays silent,
because the Editor compiles `UnityEditor` happily. Third-party packs dropped into `Assets/` are the
usual source; the preflight scans for it, preprocessor- and `.asmdef`-aware.

### What is left of Trash Dash in `MainSummaRace` (re-audited 2026-08-05)

This supersedes the old 2026-07-21 warning box, whose central claims are now **false** — re-verified file by file, not from memory:

- **`Packages/manifest.json` is clean.** No `com.unity.ads`, `com.unity.analytics`, `com.unity.purchasing`, `com.unity.microsoft.gdk(.tools)` — P0 removed them and they have not come back. `ProjectSettings/UnityConnectSettings.asset` has **every service `m_Enabled: 0`** (ads, analytics, purchasing, crash reporting, performance reporting). `Assets/Resources/` is **empty** — `BillingMode.json` is gone.
- **`UNITY_SOCIAL` is NOT defined for Android** — the old claim was wrong. Android's `scriptingDefineSymbols` is exactly `UNITY_POST_PROCESSING_STACK_V2`; `UNITY_SOCIAL` appears only under `iPhone`, which is never built.
- **No networking of any kind compiles into the player.** `Assets/Scripts/OpenURL.cs` is **deleted** (it was referenced only by their `Main.unity`/`Start.unity`, neither in Build Settings — but it was the last live `Application.OpenURL` in *player* code, and leaving it guaranteed a permanent red line on the preflight's most important check, which is how a real failure later gets read past). Re-verified since: **zero** live networking/ads/analytics/purchasing calls in player code; the only remaining `Application.OpenURL` hits are in `CFXR.WelcomeScreen` and `PrimeTween.Installer`, both `includePlatforms: ["Editor"]`. **49** ads/analytics calls remain in Trash Dash source behind `UNITY_ADS`/`UNITY_ANALYTICS`, which are **not** defined for Android — dead source, but Unity defines those symbols *automatically* the moment the matching package returns, and that has happened twice here. `SummaRace ▸ Build Preflight` now reports both classes separately, and searches prefabs as well as scenes (which is how `AdsForMission` on `AddMissionButton.prefab`, referenced by `MainSummaRace.unity`, was found — inert, but previously invisible).
- **Still in the scene, inactive**: `GameOver`, `Loadout` (×2), `Highscore`, `OpenLeaderboard`, `StoreButton`, `MissionsButton`, `Ad Button`, `Premium Button` and the Premium* displays. F46 **neutralised the five buttons** (`m_Calls: []` + `m_Interactable: 0`): they hung off `LevelLoader.LoadLevel(name)` and were one missed hide away from loading a `shop` scene that **is not in Build Settings**. Hidden is still not removed.
- **Their `UICamera/Game` chrome is load-bearing — do NOT delete it.** `GameState.UpdateUI()` dereferences `coinText`, `premiumText` and `m_LifeHearts[0..2]` every frame.
- **Their `MusicPlayer` is the race music** (DontDestroyOnLoad; `FinishRoutine` stops its sources so it doesn't bleed into Arrange). Our `AudioManager` music is unused in this scene. Score/multiplier/mission bookkeeping still ticks in `TrackManager`, invisible.
- `GameOverState`/`Leaderboard`/`HighscoreUI` are blocked *only* by our per-frame `currentLife = maxLife` restore.

Remaining promotion work is **scene cleanup only** — delete the inactive objects (GameOver + Loadout children need a code guard first) and re-audit. The packages, the services, the URLs and their scenes are already gone.

### Race world & road — the numbers, so nobody re-derives them (measured 2026-08-06)

The endless race's world is **Trash Dash's theme/zone/segment system**, not ours. Everything below
was resolved from the assets, not from memory.

- **Two themes**, and both are used since F54: `Assets/Bundles/Themes/Default/themeData.asset`
  (`themeName: Day` — note the folder is `Default`) and `Assets/Bundles/Themes/NightTime/themeData.asset`
  (`themeName: NightTime`). Schema: `Assets/Scripts/Themes/ThemeData.cs`.
- **Three zone families per theme**, indexed **0 = Industrial, 1 = Suburbs, 2 = Urban**
  (`RaceWorlds.ZoneIndustrial/ZoneSuburbs/ZoneUrban`). `ThemeZone` has only `length` and
  `prefabList` — **zones carry no name**; family identity is whatever prefabs they point at.
- **Prefab counts per family: Industrial 3 · Suburbs 4 · Urban 14**, identical in both themes ⇒
  **42 segment references** (41 distinct files: Day reuses `UrbanRoadTSection` in two zones).
  ⚠️ **Day's Industrial zone is `IndustrialWarehouse01/02` + `UrbanRoadTSection`, not
  `IndustrialWarehouse03`** — `Default/Industrial/IndustrialWarehouse03.prefab` exists on disk and
  is referenced by nothing, while NightTime's Industrial *does* use its own. The two themes are not
  guid-parallel at that slot; don't assume symmetry.
- **Segment length is authored per prefab, not a constant.** `TrackSegment.UpdateWorldLength()`
  sums the distances between `pathParent`'s children in `OnEnable`; all 42 have exactly two path
  points, so `worldLength` = straight-line entry→exit. Three discrete sizes: **~9m** (garage, all
  Urban brick/plaster walls and dumpsters), **~18m** (warehouse01, all Suburbs houses, Urban
  buildings, road intersection/T-section), **~27m** (warehouse02/03). Family means: Industrial
  **24.0m**, Suburbs **15.7m**, Urban **12.9m**.
- **A ~840m race holds ONE family for the whole run and lays ~35 segments (Industrial) to ~65
  (Urban), ~55 typical — with ten live at a time.** The "ten segments" number that appears in the
  F54 commit message is *ten spawned in their first Update*, not ten for the race; don't conflate them.
- ⚠️ **The race pacing numbers below and in the NEXT row are STALE — re-measured 2026-08-20.** The
  constants now on disk are `RaceSecondsPerGate` **20** (not 12), `RaceFirstGateDistance` **200**
  (not 130), `RaceMaxGateGap` **800** (not 300), plus `RacePreviewLeadSeconds` 12 +
  `RaceQuietRunSeconds` 6. Simulated against the real track (minSpeed 10, maxSpeed 30,
  `k_Acceleration` 0.2) the race is **1685–2201 m and 89–107 s**, not ~840 m / ~60 s, and each gate
  gives **17.1–23.2 s** of reading, not 9.0–13.7 s.
- **Owner decision D2 ("reading time for the slowest readers") is CLOSED — and it did not need a
  constant change.** The `secondsFloor` in `NextGateGap` converted seconds to metres as
  `seconds * runSpeed`, which is only correct if the speed never changes; the runner accelerates
  across the gap, so it ate the distance early and the window came in short. Measured: **hard
  delivered 16.1 s where the floor claimed 18 s** — under the ~16.5 s a 70wpm reader needs, on the
  one difficulty with the least slack. The floor now integrates (`d = v·T + ½·a·T²`, then the
  remainder at maxSpeed). Result: **easy unchanged** (its `bySpeed` always beats the floor),
  average +0.3 s on one gate, **hard a flat 18.0 s at every gate**; tightest gate anywhere is now
  17.1 s (gate 1, set by `RaceFirstGateDistance`). Do not "simplify" that back to a multiply.
- ⚠️ **Draw-call risk is bigger than the old note implies.** At 1685–2201 m a run lays roughly
  **90–170 segments** rather than the ~55 recorded below — still ten live at a time, so RAM is
  bounded, but the per-frame scenery load is unchanged and **still unmeasured on device**.
  `GameRules.RaceMaxSceneryPerSegment` (14) remains the first number to halve if the race misses 30fps.
- **The 840m is emergent, not a constant**: `RaceFirstGateDistance` 130m + four gaps of
  `RaceSecondsPerGate (12) × difficulty (1.25 / 1.0 / 0.8) × current speed`, clamped to
  `[RaceMinGateGap 110, RaceMaxGateGap 300]`, plus run-out. At `minSpeed` 10 accelerating 0.2 m/s²
  on average difficulty that is ≈ 130 + 120 + 150 + 180 + 210 + run-out ≈ 840m.
- **A world = theme + zone + sky dome + greenery + light** (`Features/Race/RaceWorlds.cs`), default
  fallback `bright_park`. The sky dome is chosen **independently of theme** (`blue_hour_suburbs` is
  Day art under the night dome):

  | # | world | theme | zone | night sky | trees | grass |
  |---|---|---|---|---|---|---|
  | 1 | `morning_suburbs` | Day | Suburbs | – | 2 | 6 |
  | 2 | `bright_park` | Day | Suburbs | – | 5 | 12 |
  | 3 | `sunset_town` | Day | Urban | – | 1 | 2 |
  | 4 | `blue_hour_suburbs` | Day | Suburbs | ✔ | 3 | 4 |
  | 5 | `overcast_industrial` | Day | Industrial | – | 0 | 1 |
  | 6 | `golden_fields` | Day | Industrial | – | 2 | 14 |
  | 7 | `night_city` | **NightTime** | Urban | ✔ | 0 | 0 |
  | 8 | `misty_morning` | Day | Urban | – | 3 | 5 |
  | 9 | `autumn_lane` | Day | Suburbs | – | 6 | 8 |
  | 10 | `starlit_finale` | **NightTime** | Suburbs | ✔ | 2 | 3 |

- **Zone forcing lives in `Features/Race/Endless/EndlessWorldDressing.cs`**, the project's only
  `[DefaultExecutionOrder(-2000)]`. It must beat `TrackManager`'s first `Update`, whose ten
  `SpawnNewSegment` coroutines each read `m_CurrentZone` before their first yield. `HoldZone()` then
  steps `ChangeZone()` a full lap (3 calls) every `GameRules.RaceZoneHoldSeconds` (5s), which is a
  no-op on the family but resets their 500m rotation counter; `HoldSky()` re-asserts the dome every
  frame (equality-tested, free in steady state).
- **`SummaRace/SkyTint`** (`Assets/_Game/Art/Shaders/SkyTint.shader`) is reached through
  `[SerializeField] Shader skyTintShader` on the director, wired in `MainSummaRace.unity` — **never
  `Shader.Find`**, which a player build would strip (invisible in the Editor, fails only on device).
  ⚠️ `RaceController.cs` (legacy race) *does* still use `Shader.Find` at lines 54/58/753; harmless
  today because nothing routes to that scene, but it is the same trap if that race is ever revived.
- **Draw-call headroom is unmeasured on device.** `GameRules.RaceMaxSceneryPerSegment` (14) is the
  documented first number to halve if the race misses 30fps.

### Teacher PIN + participant code — install rule and recovery (F45, F52)

**Install now has two teacher steps, not one:** set the PIN (below), and give each learner their
**participant code** — the researcher's own id from that child's paper booklet. The code is entered
behind the PIN (TeacherMenu ▸ participant step), is asked for *before* Name Entry on "+ New learner",
refuses duplicates, and is stamped on **every log row** as well as the export roster. Without it a
tablet's rows cannot be joined to that child's pretest/posttest score, and nothing detects it until
analysis. Full tap paths: `SummaRace_Study_Operations_Runbook.md` §1.2b.

**Set the PIN during install, before the tablet is handed to a learner.** Two matching entries
still cannot tell a teacher from a determined child; what changed is that a child claiming it
is now *recoverable* instead of terminal.

**If the PIN is lost, or a learner set one:** MainMenu → teacher corner → on the **Enter PIN**
step, **leave the PIN box empty** and **press and hold OK for 6 seconds** (both conditions are
required — `BeginHold` arms only on `GateStep.EnterPin` with an empty box). A "Reset this
tablet?" warning appears; release, tap **ERASE TABLET**, then tap again to confirm. The tablet
wipes and returns to first-run PIN setup. Back cancels at any point before the second tap.

**Cost:** every learner profile, all progress and **every log on that tablet**, permanently —
export lives behind the same PIN, so the logs cannot be retrieved afterwards. **Export before
the study ends, not after a lockout.** After 5 wrong PINs the screen imposes a 30s wait; the
hold gesture still works during it. Recovery is a hold rather than a code because a hardcoded
backdoor PIN would ship readable inside the APK.

**Known flags:**
- **The "memoriser route" residual is 25/150 pairs and ALL 25 ARE THE `SOMEBODY` SLOT. Do not try to close it.** The support-removal ladder means the Reader pre-teaches a slot with the text visible and the Race asks the same slot with the text gone — so a race answer that is byte-identical to the Reader's answer is, in principle, passable from memory rather than comprehension. `SummaRace_Critique.md` measured the race **84.0% passable from memory** (vs 34.0% for a control), `8cba732` brought it to **59.3%**, and this is what is left. Measured across all 150 page↔element pairs (normalised, case- and punctuation-insensitive): **25 exact matches, 25 of them `SOMEBODY`, 0 in any other slot.** Every one is a character's name — "Duncan", "Molly", "Chef Pierre", "Marusia". You cannot reword a name, and **remembering who the story was about IS the recall being tested**, not a leak. An automated pass told to "reduce carryover" would rename the researcher's characters, which is content vandalism. Excluding names, the race scores **49.2%** against a 33.3% chance floor — a real but much smaller effect, and the honest number to report as a limitation. The remaining decision (whether 49.2% is acceptable) is the researcher's, not the build's.
- `s01_easy.json` page-split/questions/distractors were AI-authored — the researcher's source doc (`Documentation/STORIES FOR SESSION 1-10 ….docx`) gives Day 1 **EASY** only a passage + main idea, while Day 1 AVERAGE/HARD get the full `5 PAGES:` + `Processing Question` treatment. Sign-off before the study build is still the right call (GDD D6), but as a formality: the authored questions follow the researcher's own pattern faithfully. **Do not "fix" them to be comprehension-shaped** — an earlier pass (2026-07-21) argued that and was wrong. The researcher's own processing questions are deliberately SWBST-shaped ("Who is the main character?" = Somebody, "What did Duncan want to do?" = Wanted, "What problem did the crayons have?" = But, …). The Reader **pre-teaches** the five slots with the text on screen; the Race asks the same five with the text gone. That is the support-removal ladder working — Read = scaffold → Race = unsupported recall → Arrange = sequence → Summary = produce — not redundancy. Same pattern applies to the Phase G stories. The source doc also writes options as `A. / B. / C.`, which is the case for adding letter prefixes in the Reader.
- **Nothing is missing at runtime; the gap is art *quality*, not art *presence*** (re-verified 2026-08-05 against the files): **30/30** `heroImage` paths resolve to a PNG in `Resources/Stories/Art/`, **150/150** narration clips exist in `Resources/Stories/Narration/`, and **31/31** `AudioKeys` constants are backed by a file in `Resources/Audio/` (re-counted 2026-08-19; the old "21/21" was stale). **All 30 hero images are now real art** (2026-08-19): the 27 placeholders were replaced with generated illustrations, each verified against its own story before install. Two traps, recorded because they will recur if art is redone: the generator returned **#19 and #20 swapped** (skateboard where the icebox belonged) and filenames are assigned from generation ORDER, so it would have silently mis-captioned two cards; and the art arrived **16:9 while the card forces 3:2** — `Hero` carries an **AspectRatioFitter (EnvelopeParent, ratio 1.5)** with `preserveAspect` **off**, so an uncropped 16:9 source is squashed ~16% horizontally. Each was cropped to 1412x941 from the right, which the composition allows because the prompts keep the right side clear for the title. The prompts that produced them are kept in **`missingassets/HERO_PROMPTS.md`**; the overwrite rule still stands for any replacement (same filename, keep the `.png.meta`). No image-gen provider keys are configured in MCP (`generate_image` needs a fal.ai/OpenRouter key if wanted). **Art swaps are free**: the JSON carries `heroImage`/`narration` Resources paths and `AudioManager.PlayNarration` treats a missing clip as a silent page, so dropping a correctly named file lights it up with no code change.
- ~~BitGem `cop.fbx` external-material warnings~~ **fixed** (InPrefab import + URP-converted `cop_blue.mat`); the cop rig was converted Generic→**Humanoid** in F27. ~~`Aj.fbx` renders invisible~~ **stale, disproved in F38** — Aj is now the race runner.
- Narration: **all 30 stories generated and imported** (edge-tts `en-PH-RosaNeural --rate=-10%`, 150 mono Vorbis clips in `Resources/Stories/Narration/`). Reader auto-plays per page; VOICE ON/OFF persists via `PrefKeys.NarrationOn`; `AudioManager.PlayNarration(path)`. To regenerate a page: write its text to a temp file and run `python -m edge_tts --voice en-PH-RosaNeural --rate=-10% -f <file> --write-media <id>_pN.mp3` (embedded quotes break inline `--text` in PS 5.1). Generate to a staging folder **outside `Assets/`** and import as one batch — writing straight into `Assets/` churns the asset database mid-work. A 0-byte mp3 during a batch is the clip being written, not a failure; only judge zero-length files after the batch ends. Runtime TTS stays forbidden.
- **Weather is BUILT (2026-08-20) and it is Shuriken, not either VFX pack.** `RaceWorlds.WeatherFor(worldId, out kind, out rate)` is a table keyed by world id — deliberately NOT a field on the `World` struct, because that struct is assembled by three chained builders (`Make -> Place -> Mix`) and threading one more value through all of them would touch ten recipes for a purely additive layer. `EndlessWorldDressing.BuildWeather` makes one camera-parented `ParticleSystem` with `simulationSpace = World`. **6 of 10 worlds carry weather on purpose** — rain on `overcast_industrial` (320/s) and `night_city` (240/s), motes on `misty_morning` (70), `golden_fields` (55) and `autumn_lane` (90), snow on `starlit_finale` (110); the other four stay clear because snow on a bright morning reads as a bug, not variety. Deterministic per world, so it is not a confound. **The material is `Resources.Load<Material>("FX/weather_particle")`, never `Shader.Find`** — a Resources asset is a build dependency so its shader variants survive stripping, whereas `Shader.Find` resolves in the Editor and returns null in a player (the failure that only appears on the tablet). Verified live: the load returns a real Material on `URP/Particles/Unlit`. Null material or `WeatherNone` = no weather and nothing else changes.
- **The weather pack in `Plugins/Calcatz/` CANNOT be used — do not plan around it.** `RainVFX` / `SnowVFX` / `FogVFX` are **VFX Graph** (`.vfx`) assets and `com.unity.visualeffectgraph` is **not in `Packages/manifest.json`**, so those three prefabs reference a component type that does not exist here. Installing the package would not save them either: VFX Graph needs compute shaders and **Android is pinned to OpenGLES3** (`m_BuildTargetGraphicsAPIs` = `0b000000`, set deliberately in F56 because Vulkan had never been run). Weather in the race has to be **Shuriken** (`com.unity.modules.particlesystem`, which IS installed) — a code-built ParticleSystem parented to the camera, same pattern as the race's other code-built FX. Its material must come from a **serialized reference**, never `Shader.Find`, or a player build strips it (invisible in the Editor, fails only on the tablet).
- **Eight installed packages have zero references in the project** — `visualscripting`, `timeline`, `ai.navigation`, `postprocessing` (PPv2), `vectorgraphics`, `2d.tilemap`, `multiplayer.center`, and `collab-proxy`. Removing them would cut compile and build time, but **do it after the first successful APK, not before** — the build has never once succeeded, and changing the package set is exactly the kind of variable you do not want in play while diagnosing a first build. Note `UNITY_POST_PROCESSING_STACK_V2` is in Android's scripting defines; it becomes inert if PPv2 goes.
- **Story JSONs are generated artifacts.** Content edits belong in `Tools/StoryPipeline/overrides.json` + a re-run, not in the 30 files. `s01_*` are excluded from generation as the gold standard — anything added to every story (the `world` field, for one) has to be added to those three by hand, which is exactly how they silently missed session 1's world until P4 caught it.
- ~~Race world-space labels still use legacy 3D `TextMesh`~~ — **legacy race only** (`Race.unity`, zero call sites). The shipping endless race builds its cards, tracker plaques and the F47ⓐ screen-space option preview itself in `EndlessRaceDirector`; the preview is normal UI TMP.
- Design references inside Hyper_Casual_UI `Sprites/GameUI/`: `Victory Pannel.png` (Results screen, applied), `Level screen.png` (numbered circle grid = template for Phase G SessionMap), `Main Menu.png` (glossy icon pills). The pack's demo scenes are baked showcase sprites, not prefabs; several kit sprites ship with broken import settings (not Sprite type / Multiple mode with no rects) — fix importer before use.

## Architecture (implemented — follow these patterns)

- **Singletons** (`_Game/Scripts/Core/`): `Bootstrapper` (Boot scene) creates `[Core]` GameObject with `GameManager`, `AudioManager`, `SaveManager`, `SceneLoader` (DontDestroyOnLoad). `EventBus` is static pub/sub; event payloads in `Core/GameEvents.cs` (catalog in TDD §8).
- **Features never call each other** — raise EventBus events + read `GameManager` state. Scene flow only via `SceneLoader.Instance.Load(SceneNames.X)`.
- **Constants only** (`_Game/Scripts/Constants/`): `SceneNames`, `AudioKeys`, `PrefKeys`, `GameRules` (all tuning numbers), `GameText` (all learner-facing strings). Never hard-code a scene name, audio key, gameplay number, or UI string.
- **Content pipeline** (`_Game/Scripts/Data/`): `StoryLoader.Load("s01_easy")` reads `Resources/Stories/<id>.json` → validated `StoryData`. Add a story = add a JSON, zero code. `Data/StoryIds` owns the `sNN_<difficulty>` convention — resolve an id through it, never by string-building, so StorySelect and SessionMap can't disagree. The 30 JSONs are **generated** by `Tools/StoryPipeline/` (see its README) — edit content there, not in the files.
- **Persistence + research data** (`Core/`): `SaveManager` (profiles, per-learner `.jsonl` logs, `ExportLogs` + companion roster, `DeleteAllData`), `SessionLogService` (one `SessionLog` per play-through, built from EventBus traffic only), `TeacherGate` (salted PIN hash, session unlock), `Data/ParticipantCodes` (the join key to the paper pretest/posttest — teacher-set behind the PIN, never learner-typed). Never store a raw PIN; never log anything that leaves the device. **Profile writes are atomic** (temp → `File.Replace`, `.bak` kept, read falls back to `.bak`) — do not "simplify" that back to `File.WriteAllText`; see F48 for what it cost. **Log schema version lives in one place**, `SessionLogService.SchemaVersion` — bump it and document the new fields in `SummaRace_Data_Dictionary.md` in the same pass, because the researcher analyses from that document.
- **Audio**: clips live in `Assets/_Game/Resources/Audio/` named exactly per `AudioKeys` (e.g. `sfx_correct.ogg`); `AudioManager.Instance.PlaySfx(AudioKeys.SfxCorrect)`. Swap a sound = replace the file. Raw Kenney packs in `Assets/Audio/Kenney/` are the source library.
- **Every scene controller** null-checks `GameManager.Instance`/`CurrentStory` and falls back to `StoryLoader.Load("s01_easy")` so any scene can be played directly in-editor (TDD §13).
- **Naming**: PascalCase scripts/scenes/methods, `_camelCase` privates, `[SerializeField] private` over public, snake_case asset files, namespaces `SummaRace.<Layer>` matching folders.
- **Third-party packs go in `Assets/Plugins/`** (13 packs there now). Design reference for UI skin: `Plugins/Hyper_Casual_UI/Scenes/Demo_Game_UI`. Fonts: `Assets/Art/Fonts/` Fredoka (headings) + Nunito (body); the TMP assets exist in `Art/Fonts/TMP/` (F1). Characters + Mixamo clips (all Humanoid): `Assets/Art/Characters/`.

## Workflow rules (owner's standing instructions)

- **Professional quality at every stage**: grey-box = placeholder *visuals* only; logic must always be correct, complete, sound-wired, dead-end-free. Placeholder screens allowed only as temporary "never a dead end" stubs.
- **Commit + push after every verified milestone** (small commits). Owner playtests in the Editor before a milestone counts as done.
- Grey-box first, one story before thirty, GDD wins design arguments, docs win over improvisation.

## Working with the Unity Editor (MCP)

This project has **MCP For Unity** (`com.coplaydev.unity-mcp`) installed. When a Unity Editor instance is connected, prefer the `mcp__UnityMCP__*` tools over editing serialized assets by hand:

- **Read editor/project state via MCP resources**, then mutate via tools — check state before changing it.
- Scenes/objects: `manage_scene`, `manage_gameobject`, etc. — raw `.unity`/`.prefab` YAML edits break GUID/fileID references. Complex scene builds work well via `execute_code` (C#6-ish subset; use `UnityEngine.Object.DestroyImmediate`, wire `[SerializeField]` fields via `SerializedObject`).
- C#: write files, then refresh with compile and verify the type exists in `Assembly-CSharp` before building scenes that reference it.
- Do **not** run Unity CLI `-batchmode` while the Editor is open — it locks the project.
- **Gotchas learned:**
  1. If a new `.cs` validates clean but its type never appears in `Assembly-CSharp` (compile time ~1ms, file missing from CompilationPipeline sources), Unity's incremental compiler has stale tracking → delete the `.cs`+`.meta`, refresh, re-add the file fresh.
  2. **`refresh_unity` does NOT compile** (it returns `compile_requested: false`). Follow it with `CompilationPipeline.RequestScriptCompilation()` or you will reflect over stale types and believe a wrong result.
  3. **`execute_code` runs as a method body** — `using` directives are a compile error there. Fully qualify (`System.Text.StringBuilder`, `UnityEditor.AssetDatabase`).
  4. **Trash Dash ships its own `GameManager` in the global namespace**, which outranks a using-directive, and an alias named `GameManager` is illegal for the same reason (CS0576). Use `using Core = SummaRace.Core;` and qualify.
  5. **Overlay UI cannot be judged in the Editor's game view** — it is landscape, the game is portrait-locked. Render the canvas through an offscreen camera into a RenderTexture at 720×1280 for a true portrait check. Every P2b/P3b layout bug (overflowing board, duplicated "Enter PIN" label, indistinguishable avatars, low-contrast pill) was caught this way and none of them was visible in code.
  6. **Kit sprites can be BAKED showcase art** — `Hyper_Casual_UI/Sprites/GameUI/Level.png` has "Level 23", "Score 232,00", stars and a lock *drawn into the image*, so a number layered on top stacks onto someone else's UI. **Look at any kit sprite before using it**; several also ship with broken importers (not Sprite type, or Multiple mode with no rects).
  7. Play mode pauses without Editor focus (Run In Background off) — the MCP bridge stalls; have the owner click Unity/press Play. A `Time.timeScale=0` seen while MCP-driving is usually this, not a bug.
  8. **Unity does not run `Awake`/`OnEnable` in edit mode**, so singleton `Instance`s stay null and `SessionLogService` never subscribes. Invoke the lifecycle methods by reflection or an edit-mode test silently proves nothing — that cost one wrong result in P3a.
  9. Asset-store imports land in `Assets/` root — move to `Plugins/` via `AssetDatabase.MoveAsset` (or move file+`.meta` together on disk), never a bare file move.

## Code layout & assemblies

No `.asmdef` files: runtime → `Assembly-CSharp`, editor-only (`Editor/` folders) → `Assembly-CSharp-Editor`. Introduce asmdefs before adding test assemblies or if compile times grow.

## Rendering

⚠️ **The two-tier model below is NOT what currently runs — verify before relying on it.** Measured 2026-08-05: every quality level has `customRenderPipeline: {fileID: 0}` and `GraphicsSettings.m_CustomRenderPipeline` points at **`Assets/RenderingPipeline.asset` (Trash Dash's)** with `Assets/UIRenderer.asset` as its renderer. `Mobile_RPAsset`/`Mobile_Renderer` and `PC_RPAsset`/`PC_Renderer` are **assigned to nothing**, so every tuning decision recorded for them is dead. Consequences on the 2GB floor device, in cost order: `renderScale` is **1.0**, not Mobile's 0.8 (that alone is ~36% of fragment cost); main-light shadows on with a **2048 map and 4 cascades** at 50m (four extra passes on a tiler); `m_IntermediateTextureMode: Always` on UIRenderer forces an intermediate colour target instead of direct-to-backbuffer. Do NOT simply reassign the Mobile asset — the whole F26–F42 race look was built and playtested against *their* pipeline (curved/unlit materials), so switching is a visual change that needs a playtest, not a config tidy. Note also RAM is the real pressure, not polygons: the full Daytime theme is only ~80k verts, against a once-huge pool of `DecompressOnLoad` audio (their `MusicPlayer` is `DontDestroyOnLoad` and plays six 4-minute stems at once, so it stays resident for the whole session). ✅ **`SummaRace ▸ Device Budget ▸ Apply ALL safe fixes` HAS now been run** (`c03c2cd`, `24d682e`) — an earlier version of this line said it had not, and that was stale. Re-verified on disk 2026-08-06: `STEMSMainTrackMono.ogg` and `music_race.ogg` are `loadType: 2` (Streaming), `music_menu.mp3` likewise, and the only remaining `DecompressOnLoad` clip over 1MB anywhere is `Assets/Audio/audio_hero_Story-Time_SIPML_Q-0216.mp3` (6MB), whose guid is **referenced by nothing** — it is raw pack source, not shipped. The 545 files still on `DecompressOnLoad` are the small Kenney source library. ⚠️ The pool was **220.6MB**, not the 178.2MB recorded here before — a seventh stem was missed; ~127MB recovered. Two corrections that still stand from that measurement: `music_menu.mp3` is a **320kbps stereo MP3** with an audibly gapped loop, and the **Bloom lever recorded in F23 does not exist on the shipping path** — `Assets/_Game/Art/RacePostFx.asset` has a single null entry in `components:` and `MainSummaRace.unity` has **zero** cameras with `m_RenderPostProcessing: 1` (only the dead `Race.unity` does).

Two URP tiers exist as assets (Project Settings → Quality/Graphics): `Assets/Settings/PC_RPAsset` + `PC_Renderer`, `Mobile_RPAsset` + `Mobile_Renderer`. If they are ever wired up, apply renderer/shader/post changes to **both** (or deliberately tier-gate). Device floor: 2GB Android 8, 30fps in race, APK ≤ 300MB. Android: IL2CPP, ARM64, min API 26, portrait locked.

## Input

UI needs `InputSystemUIInputModule` on the EventSystem, and our own scripts use the New Input System only (`Assets/InputSystem_Actions.inputactions`).

⚠️ **`activeInputHandler` is `2` (Both) on this branch and must stay there.** Trash Dash's
`CharacterInputController` reads **legacy** `Input.touchCount` for the race swipe; under "New only"
that throws and **touch steering dies on the tablet** — invisible on a keyboard-driven desktop test.
The preflight fails the build if it is ever set back to `1`. (`main` is `1`, which is correct there.)

Shipping race input (`Features/Race/Endless/`): **`EndlessTouchInput`** = tap a third of the screen
to go to that lane (F47ⓒ — one tap to *any* lane, where their swipe is one lane per swipe;
the two cannot be merged, since a tap needs <2% drag and their swipe fires at 1%), their swipe path
kept alongside; **`EndlessKeyboardInput`** adds WASD, arrows deliberately left to their script.
`Core/BackButtonGuard` on `[Core]` swallows Android BACK app-wide (F51). The legacy race's
`PlayerRunner` polling `Keyboard.current`/`Touchscreen.current` is **not on the shipping path**.
